---
phase: 05-engram-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/aegis-memory.sh
  - references/memory-taxonomy.md
  - workflows/pipeline/orchestrator.md
  - tests/test-memory-engram.sh
  - tests/run-all.sh
autonomous: true
requirements: [MEM-01, MEM-02]

must_haves:
  truths:
    - "Pipeline stores a structured memory entry to Engram (or local JSON fallback) after each gate passes"
    - "Pipeline retrieves relevant memories before dispatching a stage and presents them as context"
    - "If Engram is unavailable, memory operations fall back to local JSON without error"
  artifacts:
    - path: "lib/aegis-memory.sh"
      provides: "Upgraded memory library with memory_save_gate() and memory_retrieve_context() helpers"
      contains: "memory_save_gate"
    - path: "references/memory-taxonomy.md"
      provides: "Memory type mapping per stage, scoping conventions, topic_key format"
      contains: "topic_key"
    - path: "workflows/pipeline/orchestrator.md"
      provides: "New Step 4.5 (memory retrieval) and Step 5.6 (gate memory persistence)"
      contains: "Step 4.5"
    - path: "tests/test-memory-engram.sh"
      provides: "Tests for gate save, context retrieval, and fallback behavior"
      min_lines: 50
  key_links:
    - from: "workflows/pipeline/orchestrator.md"
      to: "lib/aegis-memory.sh"
      via: "Step 5.6 calls memory_save_gate() as fallback path"
      pattern: "memory_save_gate"
    - from: "workflows/pipeline/orchestrator.md"
      to: "Engram MCP tools"
      via: "Step 5.6 calls mem_save, Step 4.5 calls mem_context/mem_search"
      pattern: "mem_save|mem_search|mem_context"
---

<objective>
Upgrade the memory interface and wire Engram integration into the orchestrator for gate persistence (MEM-01) and context retrieval (MEM-02).

Purpose: The pipeline currently has a local JSON memory stub. This plan upgrades it to use Engram MCP tools when available, adds structured gate memory saves after each gate pass, and retrieves relevant context before stage dispatch. The local JSON fallback is preserved for when Engram is unavailable.

Output: Upgraded aegis-memory.sh with new helpers, memory-taxonomy.md reference, orchestrator with two new steps (4.5 and 5.6), and tests covering all paths.
</objective>

<execution_context>
@/home/ai/.claude/get-shit-done/workflows/execute-plan.md
@/home/ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-engram-integration/05-RESEARCH.md

<interfaces>
<!-- Existing memory interface in lib/aegis-memory.sh -->
memory_save(scope, key, content)    # Saves entry to {scope}.json
memory_search(scope, query, limit)  # Returns JSON array of matching entries

<!-- Existing detection in lib/aegis-detect.sh -->
detect_integrations()               # Returns JSON: {engram: {available: bool}, sparrow: {...}, codex: {...}}

<!-- Orchestrator step structure (orchestrator.md) -->
Step 1: Resolve Project
Step 2: Load or Initialize State
Step 3: Detect Integrations
Step 4: Announce Pipeline Status
Step 5: Dispatch to Current Stage (Path A: subagent, Path B: inline)
Step 5.5: Evaluate Gate
Step 6: Post-Transition

<!-- Engram MCP tools (available in Claude conversation context, NOT from bash) -->
mem_save(title, content, type, project, scope, topic_key)
mem_search(query, project, type)
mem_context(project)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Memory library upgrade, taxonomy reference, and test scaffold</name>
  <files>lib/aegis-memory.sh, references/memory-taxonomy.md, tests/test-memory-engram.sh, tests/run-all.sh</files>
  <action>
1. **Upgrade lib/aegis-memory.sh** — Add two new helper functions WITHOUT changing existing `memory_save` and `memory_search` signatures (regression safety):

   - `memory_save_gate(stage, phase, summary)` — Calls `memory_save "project" "gate-${stage}-phase-${phase}" "$summary"`. This is the local JSON fallback path for MEM-01.

   - `memory_retrieve_context(scope, terms, limit)` — Calls `memory_search "$scope" "$terms" "$limit"`. This is the local JSON fallback path for MEM-02.

   - `memory_search_bugfixes(limit)` — Calls `memory_search "project" "bugfix" "$limit"`. Used by MEM-03 (Plan 02).

   Keep the existing `memory_save` and `memory_search` functions unchanged. Add the new functions below them. Use the same python3 pattern established in Phase 1.

2. **Create references/memory-taxonomy.md** — Define the memory type mapping, scoping, and key conventions:

   - Stage-to-type default mapping table:
     | Stage | Default Memory Type | Rationale |
     |-------|-------------------|-----------|
     | intake | discovery | Captures project shape, initial findings |
     | research | architecture | Technical decisions, library choices |
     | roadmap | decision | Phase ordering, scope decisions |
     | phase-plan | decision | Plan structure, task breakdown |
     | execute | pattern | Implementation patterns established |
     | verify | bugfix | Issues found, fixes applied |
     | test-gate | bugfix | Test failures, regressions caught |
     | advance | decision | Phase completion, next phase selection |
     | deploy | config | Deployment config, environment setup |

   - Scoping rules: Use `scope: "project"` for all pipeline memories. Use `project: "{project_name}"` for Engram scoping.

   - topic_key convention: `pipeline/{stage}-phase-{N}` — enables upsert on retry (no duplicate entries per stage/phase).

   - Content format: `**What**: {outcome}\n**Why**: {purpose}\n**Where**: {key files}\n**Learned**: {findings}`

   - Rules: ONE memory per gate passage (not per file/test), use topic_key for upsert on retry, prefer curated summary over raw output.

3. **Create tests/test-memory-engram.sh** — Test the new helper functions:

   - Test `memory_save_gate` creates entry with correct key format (`gate-{stage}-phase-{N}`)
   - Test `memory_save_gate` content is stored correctly
   - Test `memory_retrieve_context` returns matching entries
   - Test `memory_retrieve_context` returns empty array when no matches
   - Test `memory_search_bugfixes` returns entries containing "bugfix" in key/content
   - Test existing `memory_save`/`memory_search` still work (regression)

   Follow the exact test pattern from tests/test-memory-stub.sh: setup/teardown with temp dirs, PASS/FAIL counters, `set -euo pipefail`, `PASS_COUNT=$((PASS_COUNT + 1))` pattern (not `((PASS_COUNT++))`).

4. **Update tests/run-all.sh** — Add `"test-memory-engram"` to the TESTS array, after `"test-memory-stub"`.
  </action>
  <verify>
    <automated>cd /home/ai/aegis && bash tests/test-memory-stub.sh && bash tests/test-memory-engram.sh</automated>
  </verify>
  <done>
    - memory_save_gate, memory_retrieve_context, memory_search_bugfixes exist in aegis-memory.sh
    - Existing memory_save/memory_search unchanged (test-memory-stub.sh passes)
    - New tests pass (test-memory-engram.sh)
    - references/memory-taxonomy.md defines type mapping, scoping, topic_key, content format
    - run-all.sh includes test-memory-engram
  </done>
</task>

<task type="auto">
  <name>Task 2: Orchestrator memory steps (Step 4.5 and Step 5.6)</name>
  <files>workflows/pipeline/orchestrator.md</files>
  <action>
Add two new steps to the orchestrator workflow. These are prompt instructions (not bash scripts) because Engram MCP tools are available to Claude at the conversation level, not from bash.

1. **Add Step 4.5 — Retrieve Memory Context** between Step 4 (Announce) and Step 5 (Dispatch):

   ```markdown
   ## Step 4.5 -- Retrieve Memory Context

   After announcing pipeline status (Step 4), retrieve relevant memories before dispatching:

   1. Check Engram availability from state integrations.
   2. **If Engram available:**
      - Call `mem_context` with project="{project_name}" to get recent session context.
      - Call `mem_search` with query="{current_stage} {project_name}" to find stage-specific memories.
      - If this is a subagent stage (Step 5 Path A), include retrieved memories in the subagent's Context section.
      - If this is an inline stage (Step 5 Path B), present memories as "Previous context:" before following the workflow.
   3. **If Engram unavailable:**
      ```bash
      source lib/aegis-memory.sh
      CONTEXT=$(memory_retrieve_context "project" "{current_stage}" 5)
      ```
      Include $CONTEXT as additional context for the stage.
   4. If no relevant memories found (empty results), proceed without injecting context — do not block.
   ```

2. **Add Step 5.6 — Persist Gate Memory** after Step 5.5 (Evaluate Gate), ONLY when gate result is "pass" or "auto-approved":

   ```markdown
   ## Step 5.6 -- Persist Gate Memory

   After gate passes (Step 5.5 result is "pass" or "auto-approved"), persist a memory of the stage outcome:

   1. Read the current stage's output/summary to extract key findings.
   2. Determine memory type from `references/memory-taxonomy.md` stage-to-type mapping table.
   3. Compose a structured summary: **What** (outcome), **Why** (purpose), **Where** (key files), **Learned** (findings).
   4. Check Engram availability from state integrations.
   5. **If Engram available:** Call `mem_save` with:
      - title: "Gate passed: {stage} — {project} phase {N}"
      - type: {from taxonomy mapping}
      - content: {structured What/Why/Where/Learned summary}
      - project: "{project_name}"
      - scope: "project"
      - topic_key: "pipeline/{stage}-phase-{N}"
   6. **If Engram unavailable:** Use bash fallback:
      ```bash
      source lib/aegis-memory.sh
      memory_save_gate "{stage}" "{phase_number}" "{structured summary}"
      ```
   7. If memory save fails (Engram error, write error), log a warning but do NOT block the pipeline. Continue to Step 6.
   ```

3. **Update the Libraries section** at the top of orchestrator.md — change the aegis-memory.sh description from "Memory interface: save/search (local JSON stub, replaced by Engram in Phase 5)" to "Memory interface: save/search with gate persistence and context retrieval (Engram MCP with local JSON fallback)".

4. **Update Step 5.5** — Add a note at the end of item 4 (Advance): "After advancing, proceed to Step 5.6 for memory persistence." This ensures the flow is: gate passes -> advance -> persist memory -> post-transition.

   Actually, correction: Step 5.6 should run BEFORE advance but AFTER gate passes. The memory should capture what happened in the stage before the state moves to the next stage. So in Step 5.5, after the gate result is "pass" or "auto-approved" and BEFORE calling `advance_stage`, insert: "Proceed to Step 5.6 (Persist Gate Memory) before advancing."

5. **Add to the Handled Scenarios table** at the bottom:
   - `| Engram memory save | Gate passes: save structured memory via MCP (or fallback). Never blocks pipeline. |`
   - `| Memory context injection | Before stage dispatch: retrieve relevant memories from Engram (or fallback). Empty results = proceed without context. |`
  </action>
  <verify>
    <automated>cd /home/ai/aegis && grep -q "Step 4.5" workflows/pipeline/orchestrator.md && grep -q "Step 5.6" workflows/pipeline/orchestrator.md && grep -q "memory_save_gate" workflows/pipeline/orchestrator.md && grep -q "mem_save" workflows/pipeline/orchestrator.md && grep -q "mem_context" workflows/pipeline/orchestrator.md && bash tests/run-all.sh</automated>
  </verify>
  <done>
    - Orchestrator has Step 4.5 (Retrieve Memory Context) with Engram MCP + fallback paths
    - Orchestrator has Step 5.6 (Persist Gate Memory) with Engram MCP + fallback paths
    - Step 5.5 directs to Step 5.6 before advancing
    - Libraries section updated to reflect Engram integration
    - Handled Scenarios table includes memory operations
    - All existing tests still pass (orchestrator changes are prompt-only, no bash behavior change)
  </done>
</task>

</tasks>

<verification>
- `bash tests/test-memory-stub.sh` — existing memory tests pass (regression)
- `bash tests/test-memory-engram.sh` — new memory helper tests pass
- `bash tests/run-all.sh` — full suite passes
- `grep -c "Step 4.5\|Step 5.6" workflows/pipeline/orchestrator.md` returns 2+
- `grep -c "memory_save_gate\|memory_retrieve_context" lib/aegis-memory.sh` returns 2+
</verification>

<success_criteria>
- MEM-01: Orchestrator Step 5.6 saves structured memory after gate pass (Engram MCP primary, local JSON fallback)
- MEM-02: Orchestrator Step 4.5 retrieves context before stage dispatch (Engram MCP primary, local JSON fallback)
- Fallback: Pipeline continues with local JSON when Engram unavailable (no crashes, no blocking)
- Regression: Existing test-memory-stub.sh passes unchanged
- New tests: test-memory-engram.sh validates helper functions
</success_criteria>

<output>
After completion, create `.planning/phases/05-engram-integration/05-01-SUMMARY.md`
</output>
