---
phase: 13-enforcement-upgrade
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - lib/aegis-evidence.sh
  - workflows/pipeline/orchestrator.md
  - references/invocation-protocol.md
  - tests/test-enforcement.sh
autonomous: true
requirements:
  - ENFC-03

must_haves:
  truths:
    - "A gate bypass generates a structured audit JSON file in .aegis/evidence/"
    - "Unsurfaced bypass entries are detected by scan_unsurfaced_bypasses"
    - "After mark_bypasses_surfaced, scan returns empty array"
    - "Orchestrator step 6.5 handles blocked return code with bypass/re-run option"
    - "Bypass surfacing is wired into pipeline startup and advance stage"
  artifacts:
    - path: "lib/aegis-evidence.sh"
      provides: "write_bypass_audit, scan_unsurfaced_bypasses, mark_bypasses_surfaced functions"
      exports: ["write_bypass_audit", "scan_unsurfaced_bypasses", "mark_bypasses_surfaced"]
    - path: "workflows/pipeline/orchestrator.md"
      provides: "Updated step 6.5 with block handling and bypass flow"
      contains: "write_bypass_audit"
    - path: "references/invocation-protocol.md"
      provides: "Updated behavioral gate description noting stage-aware enforcement"
      contains: "execute/verify/deploy"
  key_links:
    - from: "workflows/pipeline/orchestrator.md"
      to: "lib/aegis-evidence.sh"
      via: "orchestrator calls write_bypass_audit on operator bypass"
      pattern: "write_bypass_audit"
    - from: "workflows/pipeline/orchestrator.md"
      to: "lib/aegis-validate.sh"
      via: "step 6.5 passes stage name and handles return code 1"
      pattern: "validate_behavioral_gate.*CURRENT_STAGE"
    - from: "lib/aegis-evidence.sh"
      to: ".aegis/evidence/bypass-*.json"
      via: "write_bypass_audit creates audit files, scan reads them"
      pattern: "bypass-.*\\.json"
---

<objective>
Add bypass audit trail so that any gate bypass generates a persistent, queryable evidence entry that is surfaced in session summaries and advance-stage reports. Wire the enforcement flow into the orchestrator.

Purpose: Ensure bypasses cannot be silent -- every override is recorded and visible in the next session or advance report.
Output: Bypass audit functions in evidence lib, updated orchestrator flow, updated invocation protocol docs.
</objective>

<execution_context>
@/home/ai/.claude/get-shit-done/workflows/execute-plan.md
@/home/ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-enforcement-upgrade/13-RESEARCH.md
@.planning/phases/13-enforcement-upgrade/13-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: validate_behavioral_gate now returns 1 for block-mode stages -->

From lib/aegis-validate.sh (after Plan 01):
```bash
validate_behavioral_gate() {
  local return_text="${1:-}"
  local stage_name="${2:-unknown}"
  # Returns 0 if marker present or mode is warn/none
  # Returns 1 if marker absent AND mode is "block"
}

get_enforcement_mode() {
  local stage_name="${1:?...}"
  # Reads aegis-policy.json behavioral_enforcement section
  # Returns "block", "warn", or "none"
}
```

From lib/aegis-evidence.sh (existing functions):
```bash
write_evidence()        # Creates .aegis/evidence/{stage}-phase-{N}.json
validate_evidence()     # Validates evidence artifact
query_evidence()        # Scans evidence files for requirement ID
```

From workflows/pipeline/orchestrator.md (current step 6.5):
```
6.5. Validate behavioral gate: Call validate_behavioral_gate "$SUBAGENT_RETURN_TEXT".
     This is warn-only — if the subagent did not output the BEHAVIORAL_GATE_CHECK block,
     a warning is logged to stderr but the pipeline continues normally.
```

From aegis-policy.json (after Plan 01):
```json
{
  "behavioral_enforcement": {
    "execute": "block",
    "verify": "block",
    "deploy": "block",
    "research": "warn",
    "phase-plan": "warn",
    ...
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bypass audit functions to evidence library with tests</name>
  <files>lib/aegis-evidence.sh, tests/test-enforcement.sh</files>
  <behavior>
    - [ENFC-03] write_bypass_audit creates JSON file in .aegis/evidence/ with bypass- prefix
    - [ENFC-03] bypass audit file contains required fields: schema_version, type, stage, phase, policy_version, timestamp, bypass_type, reason, surfaced
    - [ENFC-03] bypass audit file has type="bypass_audit" and surfaced=false
    - [ENFC-03] scan_unsurfaced_bypasses finds bypass entries with surfaced=false
    - [ENFC-03] scan_unsurfaced_bypasses returns empty JSON array when no bypasses exist
    - [ENFC-03] mark_bypasses_surfaced sets surfaced=true on all bypass entries
    - [ENFC-03] scan_unsurfaced_bypasses returns empty array after mark_bypasses_surfaced
    - [ENFC-03] write_bypass_audit uses timestamp in filename for multiple-bypass support
  </behavior>
  <action>
1. Add 8 new test cases to `tests/test-enforcement.sh` (appended after existing ENFC-01/ENFC-02 tests). Each test uses `[ENFC-03]` prefix. Tests use a temporary `.aegis/evidence/` directory (set via AEGIS_DIR to a temp dir, same pattern as test-evidence.sh). Update TOTAL count.

2. Add three functions to the END of `lib/aegis-evidence.sh`:

   **write_bypass_audit(stage, phase, bypass_type, reason):**
   - Creates `${evidence_dir}/bypass-${stage}-phase-${phase}-${timestamp}.json`
   - Uses atomic tmp+mv write (same pattern as write_evidence)
   - JSON fields: schema_version (1.0.0), type (bypass_audit), stage, phase (int), policy_version (from AEGIS_POLICY_VERSION), timestamp (ISO-8601 UTC), bypass_type, reason, surfaced (false)
   - Returns the file path on stdout

   **scan_unsurfaced_bypasses():**
   - Globs `${evidence_dir}/bypass-*.json`
   - Parses each, collects entries where type=bypass_audit and surfaced=false
   - Returns JSON array on stdout (empty array `[]` if none found)

   **mark_bypasses_surfaced():**
   - Globs `${evidence_dir}/bypass-*.json`
   - For each with surfaced=false, sets surfaced=true
   - Uses atomic tmp+mv for each file

3. Run tests — RED phase for new tests (functions don't exist yet if doing strict TDD, but since we're adding functions and tests together, ensure tests call the functions correctly). If the functions are added in the same task, all tests should pass (GREEN).

Reference: Research doc provides complete implementation for all three functions.
  </action>
  <verify>
    <automated>cd /home/ai/aegis && bash tests/test-enforcement.sh && bash tests/test-evidence.sh</automated>
  </verify>
  <done>lib/aegis-evidence.sh exports write_bypass_audit, scan_unsurfaced_bypasses, mark_bypasses_surfaced. tests/test-enforcement.sh has 23 total tests (15 from Plan 01 + 8 new), all passing with [ENFC-03] prefixes on new tests. Existing evidence tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire enforcement and bypass flow into orchestrator and update docs</name>
  <files>workflows/pipeline/orchestrator.md, references/invocation-protocol.md</files>
  <action>
1. **Update `workflows/pipeline/orchestrator.md` step 6.5** — replace the current warn-only description with stage-aware enforcement flow:

   ```
   6.5. **Enforce behavioral gate:** Call `validate_behavioral_gate "$SUBAGENT_RETURN_TEXT" "$CURRENT_STAGE"`.
     - If returns 0: continue normally (marker present, or stage is warn/none mode).
     - If returns 1 (blocked):
       a. Display: "BLOCKED: Subagent at {stage} did not complete behavioral gate verification."
       b. Offer bypass: "Type 'bypass' to override (generates audit entry), or 're-run' to retry the stage."
       c. If 'bypass': call `write_bypass_audit "$CURRENT_STAGE" "$PHASE_NUM" "operator-override" "behavioral gate marker absent"`. Continue to Step 5.5.
       d. If 're-run' or rejection: mark stage failed, STOP.
   ```

2. **Update the bash pseudocode block** in Path A to pass `$CURRENT_STAGE` to `validate_behavioral_gate` and add the source for aegis-evidence.sh.

3. **Update the comments in the Parallel Subagent Dispatch section** — note that `validate_behavioral_gate()` now accepts stage name and may return non-zero for block-mode stages.

4. **Update the decision table** at the bottom of orchestrator.md — change "Behavioral gate validation" row from "warns on missing checklist, never fails pipeline" to "blocks at mutating stages (execute/verify/deploy), warns at read-only stages (research/phase-plan)".

5. **Add bypass surfacing to Step 2** (Load or Initialize State) — after state is loaded, add:
   ```
   source lib/aegis-evidence.sh
   UNSURFACED=$(scan_unsurfaced_bypasses)
   if [[ "$UNSURFACED" != "[]" ]]; then
     echo "WARNING: Unsurfaced gate bypasses detected:"
     echo "$UNSURFACED" | python3 -c "import json,sys; [print(f'  - {b[\"stage\"]} (phase {b[\"phase\"]}): {b[\"reason\"]}') for b in json.load(sys.stdin)]"
     mark_bypasses_surfaced
   fi
   ```

6. **Add bypass surfacing to advance stage flow** — in the Path B section for the advance stage, note that the advance stage workflow should include bypass count in its report. Add a note: "Before advancing, check `scan_unsurfaced_bypasses()` and include any unsurfaced bypasses in the advance report."

7. **Update `references/invocation-protocol.md`** — in the Behavioral Gate section, update the description to note enforcement is now stage-aware:
   - At execute/verify/deploy: missing checklist BLOCKS the pipeline (subagent must verify before mutating)
   - At research/phase-plan: missing checklist generates a WARNING only (read-only stages are not blocked)
   - Add a note that bypass generates an audit trail entry

Do NOT modify the BEHAVIORAL_GATE_CHECK marker text or the 4 checklist fields — those remain identical.
  </action>
  <verify>
    <automated>cd /home/ai/aegis && grep -q "CURRENT_STAGE" workflows/pipeline/orchestrator.md && grep -q "write_bypass_audit" workflows/pipeline/orchestrator.md && grep -q "scan_unsurfaced_bypasses" workflows/pipeline/orchestrator.md && grep -q "execute/verify/deploy" references/invocation-protocol.md && echo "All wiring checks passed"</automated>
  </verify>
  <done>Orchestrator step 6.5 passes stage name to validate_behavioral_gate, handles return code 1 with bypass/re-run option, calls write_bypass_audit on bypass. Bypass surfacing wired into Step 2 (session start) and advance stage. invocation-protocol.md documents stage-aware enforcement. Decision table updated.</done>
</task>

</tasks>

<verification>
```bash
cd /home/ai/aegis && bash tests/test-enforcement.sh && bash tests/test-behavioral-gate.sh && bash tests/test-evidence.sh && bash tests/test-policy-config.sh
```
All four test suites must pass. Additionally:
- `grep -c "write_bypass_audit\|scan_unsurfaced_bypasses\|mark_bypasses_surfaced" workflows/pipeline/orchestrator.md` should return 3+ matches
- `grep -c "ENFC-03" tests/test-enforcement.sh` should return 8+ matches (one per test)
</verification>

<success_criteria>
- write_bypass_audit creates valid JSON with all required fields and surfaced=false
- scan_unsurfaced_bypasses finds new bypass entries, returns empty after mark_bypasses_surfaced
- Orchestrator step 6.5 passes CURRENT_STAGE to validate_behavioral_gate and handles return 1
- Orchestrator has bypass/re-run flow with write_bypass_audit call
- Pipeline startup (Step 2) scans for unsurfaced bypasses
- invocation-protocol.md documents stage-aware enforcement
- All existing test suites still pass (backward compatibility)
</success_criteria>

<output>
After completion, create `.planning/phases/13-enforcement-upgrade/13-02-SUMMARY.md`
</output>
