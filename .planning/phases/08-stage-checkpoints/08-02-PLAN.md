---
phase: 08-stage-checkpoints
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - workflows/pipeline/orchestrator.md
  - references/invocation-protocol.md
autonomous: true
requirements:
  - CHKP-01
  - CHKP-02

must_haves:
  truths:
    - "After each gate pass, the orchestrator writes a checkpoint via write_checkpoint()"
    - "Checkpoint write failure does not crash the pipeline — uses || true pattern"
    - "Subagent invocations include a 'Prior Stage Context' section assembled from recent checkpoints"
    - "Pipeline init clears the checkpoints directory to prevent stale context injection"
  artifacts:
    - path: "workflows/pipeline/orchestrator.md"
      provides: "Checkpoint write after gate pass (Step 5.5-A), checkpoint clear at init (Step 2), checkpoint inject for subagents (Step 4.5)"
      contains: "aegis-checkpoint.sh"
    - path: "references/invocation-protocol.md"
      provides: "Prior Stage Context section in subagent prompt template"
      contains: "Prior Stage Context"
  key_links:
    - from: "workflows/pipeline/orchestrator.md"
      to: "lib/aegis-checkpoint.sh"
      via: "source lib/aegis-checkpoint.sh"
      pattern: "source lib/aegis-checkpoint\\.sh"
    - from: "workflows/pipeline/orchestrator.md"
      to: ".aegis/checkpoints/"
      via: "write_checkpoint call after gate pass"
      pattern: "write_checkpoint"
    - from: "references/invocation-protocol.md"
      to: "assemble_context_window"
      via: "orchestrator injects assembled context into Prior Stage Context section"
      pattern: "Prior Stage Context"
---

<objective>
Wire the checkpoint library into the orchestrator and invocation protocol so checkpoints are written after every gate pass and injected into subagent prompts.

Purpose: Connect the library from Plan 01 to the pipeline's actual execution flow. After this plan, the pipeline writes checkpoints at stage transitions and subagents receive prior-stage context automatically.
Output: Modified `orchestrator.md` (3 insertion points), modified `invocation-protocol.md` (new template section)
</objective>

<execution_context>
@/home/ai/.claude/get-shit-done/workflows/execute-plan.md
@/home/ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-stage-checkpoints/08-RESEARCH.md
@.planning/phases/08-stage-checkpoints/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — the checkpoint library API -->

From lib/aegis-checkpoint.sh (created in Plan 01):
```bash
# write_checkpoint(stage, phase, content)
# Creates .aegis/checkpoints/{stage}-phase-{phase}.md
# Returns 1 if content exceeds ~375 words (~500 tokens)
# Uses atomic tmp+mv write
write_checkpoint() { ... }

# read_checkpoint(stage, phase)
# Returns content of checkpoint file, or empty if not found
read_checkpoint() { ... }

# list_checkpoints()
# Returns checkpoint file paths, oldest first
list_checkpoints() { ... }

# assemble_context_window(current_stage, count)
# Returns last N checkpoints formatted with separators
# Returns empty string if no checkpoints exist
assemble_context_window() { ... }
```

From workflows/pipeline/orchestrator.md (integration points):
```
Step 2 — Load or Initialize State
  → INSERT: clear .aegis/checkpoints/ at pipeline init

Step 4.5 — Retrieve Memory Context
  → INSERT: assemble_context_window() call, inject into subagent prompt

Step 5.5 — Evaluate Gate
  → INSERT: write_checkpoint() call after gate passes (before Step 5.55)

Libraries section:
  → ADD: lib/aegis-checkpoint.sh to the library list
```

From references/invocation-protocol.md (template to modify):
```
## Objective
[One sentence]

## Context Files (read these first)
→ INSERT new section here: ## Prior Stage Context

## Constraints
...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire checkpoint operations into orchestrator.md</name>
  <files>workflows/pipeline/orchestrator.md</files>
  <action>
    Make 4 surgical modifications to `workflows/pipeline/orchestrator.md`. Read the full file first.

    **1. Libraries section (top of file):**
    Add to the bullet list after `lib/aegis-consult.sh`:
    ```
    - **`lib/aegis-checkpoint.sh`** -- Checkpoint persistence: write/read/list/assemble stage-boundary context snapshots
    ```

    **2. Step 2 — Load or Initialize State:**
    In the "If the state file does not exist (new project)" block, AFTER `init_state "$PROJECT_NAME"`, add:
    ```bash
    # Clear stale checkpoints from any previous pipeline run
    rm -rf "${AEGIS_DIR}/checkpoints"
    ```
    Also add the same line in the "If the state file exists" block, right after the state is loaded (before pending approval check). Add a comment: `# Clear checkpoints at pipeline start — cross-session context is Engram's job, not checkpoints'`

    **3. Step 4.5 — Retrieve Memory Context:**
    At the END of Step 4.5 (after the memory context retrieval, before Step 5), add a new subsection:
    ```markdown
    ### Checkpoint Context for Subagent Dispatch

    If the current stage is a subagent stage (research, phase-plan, execute, verify), assemble recent checkpoint context:

    \`\`\`bash
    source lib/aegis-checkpoint.sh

    PRIOR_CONTEXT=$(assemble_context_window "$CURRENT_STAGE" 3)
    # PRIOR_CONTEXT is empty string if no checkpoints exist — safe to inject unconditionally
    \`\`\`

    Include `$PRIOR_CONTEXT` in the subagent invocation prompt (Step 5 Path A) as the `## Prior Stage Context` section, placed between `## Objective` and `## Context Files`. If empty, omit the section entirely.
    ```

    **4. Step 5.5 — Evaluate Gate:**
    In item 4 "Advance (gate passed)", BEFORE the line about proceeding to Step 5.55, add a new checkpoint write block:
    ```markdown
    - **Write checkpoint:** After gate passes, persist a compact context snapshot:
      \`\`\`bash
      source lib/aegis-checkpoint.sh

      # Compose checkpoint content from stage outputs
      # The orchestrator (not the library) is responsible for content composition
      CHECKPOINT_CONTENT="**Decisions:**
    - [extracted from stage output]

    **Files changed:**
    - [paths only, never content]

    **Active constraints:**
    - [carry-forward constraints]

    **Next stage context:**
    - [what the next stage needs to know]"

      write_checkpoint "$CURRENT_STAGE" "$PHASE_NUM" "$CHECKPOINT_CONTENT" || {
        echo "[checkpoint] Warning: checkpoint write failed for ${CURRENT_STAGE}, continuing" >&2
      }
      \`\`\`
      Checkpoint write failure is non-blocking — the pipeline continues with empty context rather than crashing. The `|| { ... }` block logs a warning to stderr.
    ```

    IMPORTANT: Do NOT restructure or rewrite existing content. These are 4 targeted insertions into an existing document.
  </action>
  <verify>
    <automated>grep -c "aegis-checkpoint" workflows/pipeline/orchestrator.md</automated>
  </verify>
  <done>orchestrator.md contains: (1) aegis-checkpoint.sh in Libraries list, (2) checkpoint clear in Step 2, (3) assemble_context_window in Step 4.5, (4) write_checkpoint in Step 5.5. grep returns 4+ matches for "aegis-checkpoint".</done>
</task>

<task type="auto">
  <name>Task 2: Add Prior Stage Context section to invocation-protocol.md</name>
  <files>references/invocation-protocol.md</files>
  <action>
    Read `references/invocation-protocol.md` fully first.

    Add a new section to the Structured Prompt Template. Insert `## Prior Stage Context` between `## Objective` and `## Context Files` in the template block:

    ```
    ## Objective
    [One sentence: what the subagent must accomplish]

    ## Prior Stage Context
    [Injected by orchestrator from assemble_context_window() — contains decisions, files changed, and constraints from the last 3 stage transitions. Omit this section entirely if no checkpoints exist.]

    ## Context Files (read these first)
    ...
    ```

    Also add a bullet to the "Section Requirements" list:
    ```
    - **Prior Stage Context:** Assembled automatically by the orchestrator from `.aegis/checkpoints/`. Contains compact summaries of recent stage transitions. Subagents should treat this as background context, not authoritative source — always read actual files listed in Context Files. This section is OPTIONAL and omitted when no checkpoints exist.
    ```

    Add a new anti-pattern to the Anti-Patterns section:
    ```markdown
    ### 6. Relying on Checkpoint Context Instead of Files
    - BAD: Using checkpoint summaries as the source of truth for file contents
    - GOOD: Using checkpoint context for decision history, reading actual files for current state
    ```

    These are 3 targeted insertions. Do NOT rewrite existing content.
  </action>
  <verify>
    <automated>grep -c "Prior Stage Context" references/invocation-protocol.md</automated>
  </verify>
  <done>invocation-protocol.md contains "Prior Stage Context" in the template, in Section Requirements, and in Anti-Patterns. grep returns 3+ matches.</done>
</task>

</tasks>

<verification>
- `grep "aegis-checkpoint" workflows/pipeline/orchestrator.md` — appears in Libraries, Step 2, Step 4.5, Step 5.5
- `grep "Prior Stage Context" references/invocation-protocol.md` — appears in template, requirements, anti-patterns
- `grep "rm -rf.*checkpoints" workflows/pipeline/orchestrator.md` — checkpoint clear at pipeline init
- `grep "write_checkpoint.*|| " workflows/pipeline/orchestrator.md` — non-blocking checkpoint write
- `bash tests/run-all.sh` — full test suite still passes (no regressions from doc changes)
</verification>

<success_criteria>
- Orchestrator sources aegis-checkpoint.sh and calls write_checkpoint after every gate pass
- Checkpoint write failure is explicitly non-blocking (|| pattern with warning)
- Pipeline init clears stale checkpoints directory
- Subagent dispatch assembles last 3 checkpoints into Prior Stage Context section
- Invocation protocol template includes Prior Stage Context between Objective and Context Files
- Anti-pattern documented: checkpoint context supplements, does not replace, actual file reads
</success_criteria>

<output>
After completion, create `.planning/phases/08-stage-checkpoints/08-02-SUMMARY.md`
</output>
