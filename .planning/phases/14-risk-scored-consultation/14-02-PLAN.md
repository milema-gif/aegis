---
phase: 14-risk-scored-consultation
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - lib/aegis-evidence.sh
  - workflows/pipeline/orchestrator.md
  - tests/test-risk-consultation.sh
autonomous: true
requirements: [CONS-02, CONS-03]

must_haves:
  truths:
    - "Consultation results are persisted as structured JSON in .aegis/evidence/"
    - "High-risk stages trigger mandatory consultation even if stage config says none"
    - "Codex triggers only for critical+high-risk AND codex_opted_in"
    - "Orchestrator Step 5.55 computes risk, checks budget, persists consultation evidence"
    - "Budget is reset at pipeline startup"
  artifacts:
    - path: "lib/aegis-evidence.sh"
      provides: "write_consultation_evidence function"
      exports: ["write_consultation_evidence"]
    - path: "workflows/pipeline/orchestrator.md"
      provides: "Upgraded Step 5.55 with risk scoring and evidence persistence"
      contains: "compute_risk_score"
  key_links:
    - from: "workflows/pipeline/orchestrator.md"
      to: "lib/aegis-risk.sh"
      via: "source lib/aegis-risk.sh; compute_risk_score"
      pattern: "compute_risk_score"
    - from: "workflows/pipeline/orchestrator.md"
      to: "lib/aegis-evidence.sh"
      via: "write_consultation_evidence call after consultation"
      pattern: "write_consultation_evidence"
    - from: "workflows/pipeline/orchestrator.md"
      to: "lib/aegis-consult.sh"
      via: "check_consultation_budget and record_consultation calls"
      pattern: "check_consultation_budget"
---

<objective>
Wire risk scoring into the orchestrator and add consultation evidence persistence.

Purpose: Complete the risk-scored consultation feature by (1) adding write_consultation_evidence() to the evidence library, (2) upgrading the orchestrator's Step 5.55 to compute risk scores, use them to escalate consultation, check budget, and persist results as evidence, and (3) resetting budget at pipeline startup.

Output: Updated orchestrator workflow, consultation evidence function, complete test coverage
</objective>

<execution_context>
@/home/ai/.claude/get-shit-done/workflows/execute-plan.md
@/home/ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-risk-scored-consultation/14-RESEARCH.md
@.planning/phases/14-risk-scored-consultation/14-01-SUMMARY.md

@lib/aegis-evidence.sh
@lib/aegis-consult.sh
@lib/aegis-risk.sh
@workflows/pipeline/orchestrator.md
@tests/test-risk-consultation.sh

<interfaces>
<!-- From Plan 01 (must exist before this plan runs) -->

From lib/aegis-risk.sh:
- compute_risk_score(stage, phase) -> JSON: {"score": "low|med|high", "factors": {"file_count": N, "line_count": N, "mutation_scope": "..."}}
- embed_risk_in_evidence(stage, phase, risk_json) -> updates evidence artifact stage_specific

From lib/aegis-consult.sh (budget functions from Plan 01):
- reset_consultation_budget() -> creates fresh .aegis/consultation-budget.json
- check_consultation_budget(stage, model) -> "allowed" | "run-limit" | "stage-limit" | "codex-limit"
- record_consultation(stage, model) -> updates budget tracker

From lib/aegis-consult.sh (existing):
- get_consultation_type(stage_name) -> "none" | "routine" | "critical"
- consult_sparrow(message, use_codex, timeout_secs) -> result text
- show_consultation_banner(model, stage, result)
- build_consultation_context(stage, project_name)

From lib/aegis-evidence.sh (existing):
- write_evidence(stage, phase, files_json, requirements_json, stage_specific_json, checks_json)
- write_bypass_audit(stage, phase, reason, detail)

Consultation evidence schema (from research):
{
  "schema_version": "1.0.0",
  "type": "consultation_evidence",
  "stage": "verify",
  "phase": 3,
  "policy_version": "1.0.0",
  "timestamp": "2026-03-21T16:00:00Z",
  "model": "DeepSeek",
  "consultation_type": "routine",
  "risk_score": "high",
  "risk_factors": {"file_count": 12, "line_count": 340, "mutation_scope": "modify"},
  "query_summary": "...",
  "response_summary": "...",
  "triggered_by": "risk_escalation"
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add write_consultation_evidence to evidence library with tests</name>
  <files>lib/aegis-evidence.sh, tests/test-risk-consultation.sh</files>
  <behavior>
    - [CONS-03] write_consultation_evidence creates consultation-{stage}-phase-{N}.json in .aegis/evidence/
    - [CONS-03] Evidence file contains model, query_summary, response_summary, risk_score, consultation_type, triggered_by fields
    - [CONS-03] Evidence file contains policy_version from get_policy_version()
    - [CONS-03] Evidence file has type "consultation_evidence" (distinguishable from stage evidence and bypass audit)
    - [CONS-03] Evidence file has schema_version "1.0.0" and ISO timestamp
    - [CONS-03] Atomic tmp+mv write pattern (same as write_evidence and write_bypass_audit)
    - [CONS-03] Function returns path to created evidence file on stdout
  </behavior>
  <action>
    Add `write_consultation_evidence(stage, phase, model, query_summary, response_summary, risk_score, consultation_type, triggered_by)` to `lib/aegis-evidence.sh`:
    - Creates `.aegis/evidence/consultation-{stage}-phase-{phase}.json`
    - Schema fields: schema_version, type ("consultation_evidence"), stage, phase, policy_version (from get_policy_version), timestamp (ISO 8601), model, consultation_type, risk_score, risk_factors (parsed from risk_json if available, else empty), query_summary, response_summary, triggered_by
    - Uses atomic tmp+mv write pattern (same as write_bypass_audit)
    - Returns file path on stdout
    - All JSON via python3

    Add [CONS-03] prefixed tests to `tests/test-risk-consultation.sh` verifying all behaviors above.
  </action>
  <verify>
    <automated>bash tests/test-risk-consultation.sh</automated>
  </verify>
  <done>write_consultation_evidence creates correctly structured evidence files, all [CONS-03] tests pass, existing evidence tests still pass</done>
</task>

<task type="auto">
  <name>Task 2: Wire risk scoring and evidence persistence into orchestrator Step 5.55</name>
  <files>workflows/pipeline/orchestrator.md, tests/test-risk-consultation.sh</files>
  <action>
    Upgrade `workflows/pipeline/orchestrator.md` in two locations:

    **Step 2 (Pipeline Initialization) — add budget reset:**
    After existing initialization steps, add:
    ```
    source lib/aegis-risk.sh
    source lib/aegis-consult.sh
    reset_consultation_budget
    ```

    **Step 5.55 (External Model Consultation) — full upgrade:**
    Replace the existing Step 5.55 content with the risk-scored consultation flow from research:

    1. `source lib/aegis-risk.sh` (if not already sourced)
    2. Compute risk score: `RISK_JSON=$(compute_risk_score "$CURRENT_STAGE" "$PHASE_NUM")`
    3. Extract score: parse RISK_JSON for score field
    4. Embed risk in evidence: `embed_risk_in_evidence "$CURRENT_STAGE" "$PHASE_NUM" "$RISK_JSON"`
    5. Get consultation type: `CONSULT_TYPE=$(get_consultation_type "$CURRENT_STAGE")`
    6. Risk escalation: if risk=high AND consult_type=none, escalate to "routine", set TRIGGERED_BY="risk_escalation", else TRIGGERED_BY="configured"
    7. If consult_type is still "none" after escalation, skip to Step 5.6
    8. Check budget: `BUDGET_STATUS=$(check_consultation_budget "$CURRENT_STAGE" "$MODEL")`. If not "allowed", log and skip (consultation is advisory, never blocks)
    9. Determine model: if consult_type="critical" AND risk="high" AND codex_opted_in=true, use_codex=true. Otherwise use_codex=false. NEVER auto-invoke Codex without opt-in.
    10. Execute consultation: existing consult_sparrow call
    11. Show banner: existing show_consultation_banner call
    12. Persist evidence: `write_consultation_evidence "$CURRENT_STAGE" "$PHASE_NUM" "$MODEL" "$QUERY_SUMMARY" "$RESULT" "$RISK_SCORE" "$CONSULT_TYPE" "$TRIGGERED_BY"`
    13. Record budget: `record_consultation "$CURRENT_STAGE" "$MODEL"`

    Update the decision table at the bottom of orchestrator.md to document risk-scored consultation behavior.

    Add integration-level [CONS-02] tests to test-risk-consultation.sh that verify the escalation logic (high risk + none type -> routine consultation).
  </action>
  <verify>
    <automated>bash tests/test-risk-consultation.sh && bash tests/run-all.sh</automated>
  </verify>
  <done>Orchestrator Step 5.55 computes risk, escalates consultation for high-risk stages, checks budget, persists evidence. Step 2 resets budget at startup. All test suites pass.</done>
</task>

</tasks>

<verification>
bash tests/test-risk-consultation.sh && bash tests/test-consultation.sh && bash tests/test-evidence.sh && bash tests/test-policy-config.sh && bash tests/run-all.sh
</verification>

<success_criteria>
- write_consultation_evidence creates structured JSON in .aegis/evidence/
- Orchestrator Step 5.55 upgraded with risk scoring, escalation, budget check, evidence persistence
- Budget reset at pipeline startup (Step 2)
- Codex never fires without codex_opted_in=true
- Risk escalation: high risk + none type -> routine consultation
- All test suites pass (run-all.sh)
- [CONS-02] and [CONS-03] requirement traceability on all new tests
</success_criteria>

<output>
After completion, create `.planning/phases/14-risk-scored-consultation/14-02-SUMMARY.md`
</output>
