---
phase: 14-risk-scored-consultation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/aegis-risk.sh
  - lib/aegis-consult.sh
  - aegis-policy.json
  - templates/aegis-policy.default.json
  - tests/test-risk-consultation.sh
  - tests/run-all.sh
autonomous: true
requirements: [CONS-01, CONS-02]

must_haves:
  truths:
    - "compute_risk_score returns low/med/high based on evidence artifact analysis"
    - "Risk thresholds are read from policy config, not hardcoded"
    - "Budget tracking functions enforce per-run and per-stage consultation limits"
    - "All risk and budget functions degrade gracefully when evidence or policy is missing"
  artifacts:
    - path: "lib/aegis-risk.sh"
      provides: "Risk scoring library"
      exports: ["compute_risk_score", "embed_risk_in_evidence"]
    - path: "tests/test-risk-consultation.sh"
      provides: "Risk scoring and budget tracking test suite"
      min_lines: 80
    - path: "aegis-policy.json"
      provides: "risk_thresholds and consultation_budget sections"
      contains: "risk_thresholds"
  key_links:
    - from: "lib/aegis-risk.sh"
      to: "aegis-policy.json"
      via: "python3 json.load for risk_thresholds"
      pattern: "risk_thresholds"
    - from: "lib/aegis-risk.sh"
      to: ".aegis/evidence/{stage}-phase-{N}.json"
      via: "python3 reads evidence artifact for file count/line count"
      pattern: "files_changed"
    - from: "lib/aegis-consult.sh"
      to: ".aegis/consultation-budget.json"
      via: "budget tracking functions read/write JSON"
      pattern: "consultation-budget"
---

<objective>
Build the risk scoring library and budget tracking functions for Phase 14 risk-scored consultation.

Purpose: Provide the compute_risk_score() function that analyzes stage evidence artifacts (file count, line count, mutation scope) and returns a low/med/high risk classification. Add budget tracking to consultation library to enforce per-run and per-stage consultation limits. Extend policy config with risk thresholds and budget caps.

Output: lib/aegis-risk.sh (new), budget functions in lib/aegis-consult.sh, policy config extensions, test suite
</objective>

<execution_context>
@/home/ai/.claude/get-shit-done/workflows/execute-plan.md
@/home/ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-risk-scored-consultation/14-RESEARCH.md

@lib/aegis-evidence.sh
@lib/aegis-consult.sh
@lib/aegis-policy.sh
@aegis-policy.json
@templates/aegis-policy.default.json
@tests/test-evidence.sh
@tests/test-consultation.sh

<interfaces>
<!-- Existing functions the executor needs to know about -->

From lib/aegis-evidence.sh:
- write_evidence(stage, phase, files_json, requirements_json, stage_specific_json, checks_json)
- validate_evidence(stage, phase)
- query_evidence(requirement_id)
- write_bypass_audit(stage, phase, reason, detail)

From lib/aegis-consult.sh:
- get_consultation_type(stage_name) -> "none" | "routine" | "critical"
- consult_sparrow(message, use_codex, timeout_secs) -> result text
- show_consultation_banner(model, stage, result)
- build_consultation_context(stage, project_name) -> context string

From lib/aegis-policy.sh:
- load_policy() -> validates and loads aegis-policy.json
- get_policy_version() -> returns policy version string

Evidence artifact schema (files_changed array):
  [{"path": "src/file.ts", "action": "modified", "hash": "sha256..."}]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create risk scoring library with policy config and test scaffold</name>
  <files>lib/aegis-risk.sh, aegis-policy.json, templates/aegis-policy.default.json, tests/test-risk-consultation.sh, tests/run-all.sh</files>
  <behavior>
    - [CONS-01] compute_risk_score with 3 low-risk files returns score "low"
    - [CONS-01] compute_risk_score with 12 files returns score "high" (exceeds file_count.high threshold)
    - [CONS-01] compute_risk_score with 5 files and 300 lines returns "high" (line_count exceeds threshold)
    - [CONS-01] compute_risk_score with mutation_scope "deploy" returns "high"
    - [CONS-01] compute_risk_score with mutation_scope "read_only" returns "low"
    - [CONS-01] compute_risk_score with no evidence file returns "low" (graceful fallback)
    - [CONS-01] compute_risk_score uses max() aggregation (highest factor wins)
    - [CONS-01] embed_risk_in_evidence updates evidence artifact's stage_specific.risk_score field
    - [CONS-01] risk_thresholds section exists in policy config and is loadable
  </behavior>
  <action>
    1. Add `risk_thresholds` section to `aegis-policy.json` with thresholds from research:
       - file_count: { "low": 3, "high": 10 }
       - line_count: { "low": 50, "high": 200 }
       - mutation_scope: { "read_only": "low", "create": "med", "modify": "med", "delete": "high", "deploy": "high" }
    2. Sync same section to `templates/aegis-policy.default.json`
    3. Create `lib/aegis-risk.sh` with:
       - `compute_risk_score(stage, phase)` — reads evidence artifact, counts files_changed entries, sums line counts (from file sizes or evidence data), classifies mutation scope from action values, applies policy thresholds, returns JSON: {"score": "low|med|high", "factors": {"file_count": N, "line_count": N, "mutation_scope": "..."}}
       - `embed_risk_in_evidence(stage, phase, risk_json)` — reads existing evidence artifact, adds risk_score and risk_factors to stage_specific field, writes back with atomic tmp+mv
       - When evidence file missing: return {"score": "low", "factors": {...}} with zeros
       - When policy file missing: use hardcoded defaults (same values as policy)
    4. Create `tests/test-risk-consultation.sh` with tests for all behaviors above using the project's existing assert pattern (see tests/test-evidence.sh for the pattern)
    5. Add test-risk-consultation to `tests/run-all.sh` test suite array

    Follow existing lib patterns: set -euo pipefail, source aegis-policy.sh, python3 for all JSON. Use [CONS-01] prefix on all test names for requirement traceability.
  </action>
  <verify>
    <automated>bash tests/test-risk-consultation.sh</automated>
  </verify>
  <done>compute_risk_score returns correct low/med/high for all factor combinations, embed_risk_in_evidence updates evidence artifacts, policy config has risk_thresholds section, all tests pass with [CONS-01] traceability</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add budget tracking functions to consultation library</name>
  <files>lib/aegis-consult.sh, aegis-policy.json, templates/aegis-policy.default.json, tests/test-risk-consultation.sh</files>
  <behavior>
    - [CONS-02] reset_consultation_budget creates fresh budget tracker with zero counts
    - [CONS-02] check_consultation_budget returns "allowed" when under all limits
    - [CONS-02] check_consultation_budget returns "run-limit" when total exceeds max_consultations_per_run
    - [CONS-02] check_consultation_budget returns "stage-limit" when stage exceeds max_per_stage
    - [CONS-02] check_consultation_budget returns "codex-limit" when codex exceeds codex_max_per_run
    - [CONS-02] record_consultation increments total and per-stage counts
    - [CONS-02] record_consultation increments codex count when model is "codex"
    - [CONS-02] consultation_budget section exists in policy config
  </behavior>
  <action>
    1. Add `consultation_budget` section to `aegis-policy.json`:
       - max_consultations_per_run: 10
       - max_per_stage: 2
       - codex_max_per_run: 3
    2. Sync to `templates/aegis-policy.default.json`
    3. Add to `lib/aegis-consult.sh`:
       - `reset_consultation_budget()` — creates/overwrites `.aegis/consultation-budget.json` with {"total_consultations": 0, "codex_consultations": 0, "per_stage": {}}. Uses atomic tmp+mv.
       - `check_consultation_budget(stage)` — reads budget tracker and policy limits, returns "allowed" | "run-limit" | "stage-limit" | "codex-limit". Second optional arg `model` defaults to "deepseek"; if "codex", also checks codex limit.
       - `record_consultation(stage, model)` — reads budget tracker, increments total_consultations, increments per_stage[stage], if model contains "codex" increments codex_consultations. Atomic tmp+mv write.
    4. Add [CONS-02] prefixed tests to `tests/test-risk-consultation.sh` for all behaviors above

    Budget tracker file lives at `${AEGIS_DIR:-.aegis}/consultation-budget.json`. All JSON via python3. Follow existing atomic write pattern from write_bypass_audit.
  </action>
  <verify>
    <automated>bash tests/test-risk-consultation.sh</automated>
  </verify>
  <done>Budget tracking functions enforce per-run, per-stage, and codex limits. Policy config has consultation_budget section. All tests pass including new [CONS-02] tests.</done>
</task>

</tasks>

<verification>
bash tests/test-risk-consultation.sh && bash tests/test-policy-config.sh && bash tests/test-evidence.sh
</verification>

<success_criteria>
- lib/aegis-risk.sh exists with compute_risk_score and embed_risk_in_evidence
- Budget functions (reset, check, record) exist in lib/aegis-consult.sh
- aegis-policy.json has risk_thresholds and consultation_budget sections
- templates/aegis-policy.default.json matches aegis-policy.json structure
- All tests pass with [CONS-01] and [CONS-02] requirement traceability
- Existing test suites (policy-config, evidence, consultation) still pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-risk-scored-consultation/14-01-SUMMARY.md`
</output>
