---
phase: 09-behavioral-gate
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test-behavioral-gate.sh
  - references/invocation-protocol.md
  - lib/aegis-validate.sh
  - tests/run-all.sh
autonomous: true
requirements: [AGENT-01, AGENT-02]

must_haves:
  truths:
    - "Every subagent invocation prompt includes a behavioral gate preamble with pre-action checklist"
    - "Orchestrator can detect whether a subagent completed the behavioral gate checklist"
    - "Missing checklist generates a warning, never a hard failure"
  artifacts:
    - path: "references/invocation-protocol.md"
      provides: "Behavioral Gate section with BEHAVIORAL_GATE_CHECK marker template"
      contains: "BEHAVIORAL_GATE_CHECK"
    - path: "lib/aegis-validate.sh"
      provides: "validate_behavioral_gate() function"
      exports: ["validate_behavioral_gate"]
    - path: "tests/test-behavioral-gate.sh"
      provides: "Unit tests for behavioral gate validation"
      min_lines: 80
  key_links:
    - from: "lib/aegis-validate.sh"
      to: "references/invocation-protocol.md"
      via: "BEHAVIORAL_GATE_CHECK marker pattern"
      pattern: "BEHAVIORAL_GATE_CHECK"
---

<objective>
Build the behavioral gate protocol and validation function for subagent invocations.

Purpose: Enforce read-before-edit behavior in subagents by injecting a mandatory pre-action checklist into every invocation prompt and providing a warn-only validation function that detects checklist compliance.

Output: Modified invocation-protocol.md with gate preamble, modified aegis-validate.sh with validate_behavioral_gate(), new test file with full coverage.
</objective>

<execution_context>
@/home/ai/.claude/get-shit-done/workflows/execute-plan.md
@/home/ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-behavioral-gate/09-RESEARCH.md
@references/invocation-protocol.md
@lib/aegis-validate.sh

<interfaces>
<!-- Key contracts the executor needs -->

From lib/aegis-validate.sh (existing functions):
```bash
validate_subagent_output(stage_name, expected_files...)
# Returns 0 if all files exist, 1 if any missing

validate_sparrow_result(result_text)
# Returns 0 if valid, 1 if empty or error pattern
```

From references/invocation-protocol.md (existing template structure):
```
## Objective
## Prior Stage Context
## Context Files (read these first)
## Constraints
## Success Criteria
## Output
```

The behavioral gate preamble goes BEFORE ## Objective in the template.
The BEHAVIORAL_GATE_CHECK marker must be a distinct text block the subagent outputs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create behavioral gate test suite and implement protocol + validation (TDD)</name>
  <files>tests/test-behavioral-gate.sh, references/invocation-protocol.md, lib/aegis-validate.sh, tests/run-all.sh</files>
  <behavior>
    - Test 1: invocation-protocol.md contains "## Behavioral Gate" section
    - Test 2: Behavioral Gate section appears before "## Objective" in the template
    - Test 3: Behavioral Gate section contains all 4 checklist fields (files_read, drift_check, scope, risk)
    - Test 4: Template includes BEHAVIORAL_GATE_CHECK marker
    - Test 5: validate_behavioral_gate returns 0 when input contains BEHAVIORAL_GATE_CHECK marker
    - Test 6: validate_behavioral_gate returns 0 when input does NOT contain marker (warn-only)
    - Test 7: validate_behavioral_gate writes warning to stderr when marker absent
    - Test 8: validate_behavioral_gate produces no stderr when marker present
    - Test 9: validate_behavioral_gate handles empty string input (returns 0, warns)
    - Test 10: validate_behavioral_gate handles multiline input with marker embedded
  </behavior>
  <action>
    RED phase:
    1. Create `tests/test-behavioral-gate.sh` following the project convention (see test-checkpoints.sh for pattern: setup/teardown, PASS_COUNT/FAIL_COUNT, pass()/fail() helpers).
    2. Write all 10 tests listed in behavior. Tests 1-4 are file content tests on invocation-protocol.md. Tests 5-10 are function tests on validate_behavioral_gate().
    3. Source `lib/aegis-validate.sh` in the test file.
    4. Add `test-behavioral-gate` to the TESTS array in `tests/run-all.sh` (insert before `test-pipeline-integration` which should remain last).
    5. Run tests -- all 10 must FAIL (RED).
    6. Commit: `test(09-01): add failing behavioral gate tests`

    GREEN phase:
    1. Add "## Behavioral Gate" section to `references/invocation-protocol.md` BEFORE the existing `## Structured Prompt Template` section. Content:
       - Title: `## Behavioral Gate (MANDATORY -- complete before any Edit or Write)`
       - Instructions: Read every file listed in Context Files. Fill the checklist. Output the completed checklist before proceeding to edits.
       - Checklist template with 4 fields:
         ```
         BEHAVIORAL_GATE_CHECK
         - files_read: [list every file path you read]
         - drift_check: [differences found between expected and actual state, or "none"]
         - scope: [exactly what you will change and why]
         - risk: [low/med/high — med+ means flag to orchestrator]
         ```
       - Note: "This checklist is mandatory but non-blocking. Missing it generates a warning, not a pipeline failure."
    2. Add `validate_behavioral_gate()` function to `lib/aegis-validate.sh`. Implementation:
       ```bash
       validate_behavioral_gate() {
         local return_text="${1:-}"
         if echo "$return_text" | grep -q "BEHAVIORAL_GATE_CHECK"; then
           return 0
         fi
         echo "BEHAVIORAL GATE WARNING: subagent did not output BEHAVIORAL_GATE_CHECK checklist" >&2
         return 0
       }
       ```
       Key: ALWAYS returns 0. Warning goes to stderr only.
    3. Run tests -- all 10 must PASS (GREEN).
    4. Run full suite: `bash tests/run-all.sh` -- no regressions.
    5. Commit: `feat(09-01): add behavioral gate protocol and validation function`
  </action>
  <verify>
    <automated>bash tests/test-behavioral-gate.sh && bash tests/run-all.sh</automated>
  </verify>
  <done>
    - invocation-protocol.md has Behavioral Gate section before Objective with all 4 checklist fields and BEHAVIORAL_GATE_CHECK marker
    - aegis-validate.sh exports validate_behavioral_gate() that always returns 0 and warns on missing marker
    - 10/10 tests pass, full suite green with no regressions
  </done>
</task>

</tasks>

<verification>
- `bash tests/test-behavioral-gate.sh` exits 0 with 10/10 passed
- `bash tests/run-all.sh` exits 0 with no regressions
- `grep "BEHAVIORAL_GATE_CHECK" references/invocation-protocol.md` finds the marker
- `grep "validate_behavioral_gate" lib/aegis-validate.sh` finds the function
</verification>

<success_criteria>
- Behavioral Gate section exists in invocation-protocol.md with 4 checklist fields
- validate_behavioral_gate() exists in aegis-validate.sh, always returns 0, warns on missing marker
- All tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/09-behavioral-gate/09-01-SUMMARY.md`
</output>
