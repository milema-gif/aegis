---
phase: 09-behavioral-gate
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - workflows/pipeline/orchestrator.md
autonomous: true
requirements: [AGENT-01, AGENT-02, AGENT-03]

must_haves:
  truths:
    - "Orchestrator prepends behavioral gate preamble to every subagent invocation"
    - "Orchestrator calls validate_behavioral_gate() after subagent returns and logs warnings"
    - "Parallel subagent dispatch uses batch approval or auto-approve-on-scope-match"
  artifacts:
    - path: "workflows/pipeline/orchestrator.md"
      provides: "Behavioral gate wiring in Step 5 Path A, parallel dispatch guidance"
      contains: "validate_behavioral_gate"
  key_links:
    - from: "workflows/pipeline/orchestrator.md"
      to: "lib/aegis-validate.sh"
      via: "validate_behavioral_gate() call after subagent return"
      pattern: "validate_behavioral_gate"
    - from: "workflows/pipeline/orchestrator.md"
      to: "references/invocation-protocol.md"
      via: "Behavioral gate preamble injection reference"
      pattern: "Behavioral Gate"
---

<objective>
Wire the behavioral gate into the orchestrator pipeline and add parallel dispatch guidance.

Purpose: Ensure every subagent invocation in the Aegis pipeline includes the behavioral gate preamble and that validation occurs on return, while parallel dispatch is not serialized by per-agent approval.

Output: Modified orchestrator.md with gate injection, validation call, and batch approval/auto-approve-on-scope-match documentation.
</objective>

<execution_context>
@/home/ai/.claude/get-shit-done/workflows/execute-plan.md
@/home/ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-behavioral-gate/09-RESEARCH.md
@.planning/phases/09-behavioral-gate/09-01-SUMMARY.md
@workflows/pipeline/orchestrator.md
@lib/aegis-validate.sh
@references/invocation-protocol.md

<interfaces>
<!-- From Plan 01 outputs -->

From lib/aegis-validate.sh (new function added by Plan 01):
```bash
validate_behavioral_gate(return_text)
# Checks for BEHAVIORAL_GATE_CHECK marker in subagent return text
# ALWAYS returns 0
# Writes warning to stderr if marker absent
```

From references/invocation-protocol.md (new section added by Plan 01):
```
## Behavioral Gate (MANDATORY -- complete before any Edit or Write)
[Pre-action checklist with BEHAVIORAL_GATE_CHECK marker template]
```

From workflows/pipeline/orchestrator.md (existing Step 5 Path A structure):
```
1. Read the stage workflow file
2. Resolve the agent name
3. Resolve the model
4. Build the invocation prompt (using invocation-protocol.md template)
5. Dispatch via Agent tool
6. validate_subagent_output() on return
7. If validation passes: fall through to Step 5.5
8. If validation fails: log failure, mark stage failed, STOP
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire behavioral gate into orchestrator Step 5 Path A</name>
  <files>workflows/pipeline/orchestrator.md</files>
  <action>
    Modify `workflows/pipeline/orchestrator.md` with three surgical changes:

    **Change 1: Gate preamble injection (Step 5 Path A, step 4)**
    After "Build the invocation prompt using the template from references/invocation-protocol.md", add:
    - "The behavioral gate preamble from references/invocation-protocol.md is part of the template. Every subagent invocation automatically includes the Behavioral Gate section at the top of the prompt, before ## Objective. The orchestrator fills in the declared scope (from the stage workflow's expected output files) so the subagent can compare its assessed scope against the declared scope."

    **Change 2: Gate validation on return (Step 5 Path A, after step 6)**
    After `validate_subagent_output` call (step 6), add a new step 6.5:
    ```
    6.5. **Validate behavioral gate:** Call `validate_behavioral_gate "$SUBAGENT_RETURN_TEXT"`.
         This is warn-only -- if the subagent did not output the BEHAVIORAL_GATE_CHECK block,
         a warning is logged to stderr but the pipeline continues normally.
         If the warning fires, the orchestrator notes it in the stage's gate memory
         (Step 5.6) as "behavioral_gate: missing" for audit trail.
    ```

    **Change 3: Parallel dispatch and batch approval (new subsection in Step 5)**
    Add a new subsection "### Parallel Subagent Dispatch" after Path B. Content:

    ```markdown
    ### Parallel Subagent Dispatch

    When the orchestrator dispatches multiple subagents in the same execution wave
    (e.g., during the execute stage with parallel plans):

    **Behavioral Gate in Parallel Mode:**
    1. Each subagent receives the full behavioral gate preamble independently.
    2. Each subagent outputs its own BEHAVIORAL_GATE_CHECK block.
    3. The orchestrator collects all checklist outputs after all subagents return.
    4. `validate_behavioral_gate()` is called for each subagent's return text individually.

    **Batch Approval:**
    When multiple subagents complete in the same wave, the orchestrator presents
    all scope declarations together as a single batch review, not N sequential
    approval prompts. The operator sees one summary of all scopes and approves
    or rejects the batch.

    **Auto-Approve-on-Scope-Match:**
    If every subagent's reported scope (from its BEHAVIORAL_GATE_CHECK block)
    matches the declared scope (from the orchestrator's task description), approval
    is automatic -- no operator prompt required. Scope matches when:
    - Reported files are a subset of or equal to declared files
    - Change type is consistent (create, modify, delete matches the task intent)

    If any subagent's scope diverges from the declared scope, the entire batch
    is flagged for operator review. This prevents silent scope creep while avoiding
    approval fatigue for well-scoped parallel work.
    ```

    **Change 4: Update Handled Scenarios table**
    Add three new rows:
    ```
    | Behavioral gate in subagent prompt | Every Agent dispatch includes Behavioral Gate preamble from invocation-protocol.md |
    | Behavioral gate validation | validate_behavioral_gate() warns on missing checklist, never fails pipeline |
    | Parallel dispatch batch approval | Multiple subagents in same wave get batch approval; auto-approve when scope matches |
    ```

    **Change 5: Update bash code block in Step 5 Path A**
    After the `validate_subagent_output` comment, add:
    ```bash
    #    validate_behavioral_gate "$SUBAGENT_RETURN_TEXT"  # warn-only, never fails
    ```
  </action>
  <verify>
    <automated>grep -q "validate_behavioral_gate" workflows/pipeline/orchestrator.md && grep -q "batch" workflows/pipeline/orchestrator.md && grep -q "scope-match\|auto-approve" workflows/pipeline/orchestrator.md && bash tests/test-behavioral-gate.sh && bash tests/run-all.sh</automated>
  </verify>
  <done>
    - orchestrator.md Step 5 Path A references behavioral gate preamble injection
    - orchestrator.md Step 5 Path A calls validate_behavioral_gate after subagent return
    - orchestrator.md documents batch approval and auto-approve-on-scope-match for parallel dispatch
    - orchestrator.md Handled Scenarios table includes behavioral gate rows
    - All existing tests pass with no regressions
  </done>
</task>

</tasks>

<verification>
- `grep "validate_behavioral_gate" workflows/pipeline/orchestrator.md` finds the validation call
- `grep "Behavioral Gate" workflows/pipeline/orchestrator.md` finds the preamble reference
- `grep -i "batch.*approval\|auto-approve" workflows/pipeline/orchestrator.md` finds parallel dispatch guidance
- `bash tests/run-all.sh` exits 0 with no regressions
</verification>

<success_criteria>
- Orchestrator Step 5 Path A injects behavioral gate preamble and validates on return
- Parallel dispatch documented with batch approval and auto-approve-on-scope-match
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/09-behavioral-gate/09-02-SUMMARY.md`
</output>
