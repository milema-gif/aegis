---
phase: 04-subagent-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/aegis-researcher.md
  - .claude/agents/aegis-planner.md
  - .claude/agents/aegis-executor.md
  - .claude/agents/aegis-verifier.md
  - .claude/agents/aegis-deployer.md
  - references/model-routing.md
  - references/invocation-protocol.md
  - lib/aegis-validate.sh
  - tests/test-subagent-dispatch.sh
  - tests/run-all.sh
autonomous: true
requirements: [MDL-03, MDL-04]

must_haves:
  truths:
    - "5 subagent definitions exist with correct YAML frontmatter (name, description, tools, model, permissionMode)"
    - "Model routing table maps each agent role to a model with rationale and fallback"
    - "Invocation protocol template defines structured prompt format for all subagent dispatches"
    - "Output validation library can check file existence after subagent returns"
    - "GPT-4 Mini delegation pattern documented with Sparrow invocation and timeout/fallback"
    - "Test suite validates agent definitions, routing table, invocation protocol, and validation library"
  artifacts:
    - path: ".claude/agents/aegis-researcher.md"
      provides: "Researcher subagent definition"
      contains: "name: aegis-researcher"
    - path: ".claude/agents/aegis-planner.md"
      provides: "Planner subagent definition"
      contains: "name: aegis-planner"
    - path: ".claude/agents/aegis-executor.md"
      provides: "Executor subagent definition"
      contains: "name: aegis-executor"
    - path: ".claude/agents/aegis-verifier.md"
      provides: "Verifier subagent definition"
      contains: "name: aegis-verifier"
    - path: ".claude/agents/aegis-deployer.md"
      provides: "Deployer subagent definition"
      contains: "name: aegis-deployer"
    - path: "references/model-routing.md"
      provides: "Agent-to-model routing table"
      contains: "aegis-researcher"
    - path: "references/invocation-protocol.md"
      provides: "Structured prompt template for subagent dispatch"
      contains: "## Objective"
    - path: "lib/aegis-validate.sh"
      provides: "Output validation functions"
      exports: ["validate_subagent_output"]
    - path: "tests/test-subagent-dispatch.sh"
      provides: "Subagent system test suite"
      min_lines: 80
  key_links:
    - from: "references/model-routing.md"
      to: ".claude/agents/*.md"
      via: "model field in agent frontmatter matches routing table"
      pattern: "model:"
    - from: "references/invocation-protocol.md"
      to: "references/model-routing.md"
      via: "protocol references routing table for model resolution"
      pattern: "model-routing"
    - from: "lib/aegis-validate.sh"
      to: ".claude/agents/*.md"
      via: "validates output from subagents defined in agent files"
      pattern: "validate_subagent_output"
---

<objective>
Create the subagent foundation: 5 agent definition files, model routing table, invocation protocol, output validation library, and tests.

Purpose: Establishes all contracts and artifacts that the orchestrator and stage workflows will reference when dispatching subagents (Plan 02).
Output: `.claude/agents/` directory with 5 agent definitions, `references/model-routing.md`, `references/invocation-protocol.md`, `lib/aegis-validate.sh`, `tests/test-subagent-dispatch.sh`
</objective>

<execution_context>
@/home/ai/.claude/get-shit-done/workflows/execute-plan.md
@/home/ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-subagent-system/04-RESEARCH.md
@.planning/phases/04-subagent-system/04-VALIDATION.md
@lib/aegis-detect.sh
@tests/test-stage-workflows.sh

<interfaces>
<!-- Existing integration detection (Sparrow availability check) used for GPT-4 Mini routing -->
From lib/aegis-detect.sh:
```bash
detect_integrations()  # Returns JSON with sparrow.available, engram.available, codex.available
```

From references/state-transitions.md:
```
# Stage order: intake, research, roadmap, phase-plan, execute, verify, test-gate, advance, deploy
```

Test pattern from tests/test-stage-workflows.sh:
```bash
pass() { echo "PASS: $1"; PASS_COUNT=$((PASS_COUNT + 1)); }
fail() { echo "FAIL: $1 — $2"; FAIL_COUNT=$((FAIL_COUNT + 1)); }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create subagent definitions, routing table, invocation protocol, and validation library</name>
  <files>
    .claude/agents/aegis-researcher.md
    .claude/agents/aegis-planner.md
    .claude/agents/aegis-executor.md
    .claude/agents/aegis-verifier.md
    .claude/agents/aegis-deployer.md
    references/model-routing.md
    references/invocation-protocol.md
    lib/aegis-validate.sh
  </files>
  <action>
    Create `.claude/agents/` directory and 5 subagent Markdown files with YAML frontmatter.

    Each agent file follows this structure:
    ```yaml
    ---
    name: aegis-{role}
    description: {trigger description for orchestrator delegation}
    tools: {minimal tool list per role}
    model: {from routing table}
    permissionMode: {bypassPermissions for executor/deployer, dontAsk for researcher/verifier}
    maxTurns: {per role}
    ---

    System prompt body with:
    1. Role identity
    2. Instructions for reading context files from prompt
    3. Expected output format (write to files, return brief summary)
    4. Constraint: cannot spawn sub-subagents
    ```

    **Agent-specific configurations:**

    | Agent | Tools | Model | permissionMode | maxTurns |
    |-------|-------|-------|----------------|----------|
    | aegis-researcher | Read, Grep, Glob, Bash, WebFetch | sonnet | dontAsk | 50 |
    | aegis-planner | Read, Write, Edit, Bash, Grep, Glob | inherit | bypassPermissions | 50 |
    | aegis-executor | Read, Write, Edit, Bash, Grep, Glob | sonnet | bypassPermissions | 80 |
    | aegis-verifier | Read, Bash, Grep, Glob | sonnet | dontAsk | 40 |
    | aegis-deployer | Read, Write, Edit, Bash, Grep, Glob | sonnet | bypassPermissions | 60 |

    Each agent's system prompt must instruct: (1) read all files in "Context Files" section, (2) follow stage workflow actions, (3) write outputs to specified paths, (4) return a structured completion message listing files created/modified, success criteria met, and issues encountered. Explicitly state: "You CANNOT spawn sub-subagents. All work must be completed within this session."

    **Create `references/model-routing.md`:**

    A reference document with:
    - Routing table mapping agent roles to models, rationale, and fallbacks
    - GPT-4 Mini (DeepSeek via Sparrow) routing rules: which tasks qualify (formatting, summarizing, boilerplate -- NOT reasoning, architecture, code logic)
    - Sparrow invocation pattern: `/home/ai/scripts/sparrow "task description"` with `--timeout 60`
    - Fallback behavior: if Sparrow unavailable, skip external delegation and continue
    - Cost note: Sparrow default is DeepSeek (free), `--codex` is GPT-5.3 Codex (paid, user-explicit only per CLAUDE.md)
    - Three routing profiles: quality (all opus/sonnet), balanced (default -- mixed), budget (haiku + DeepSeek)

    Include this exact routing table:

    | Agent Role | Model | Rationale | Fallback |
    |------------|-------|-----------|----------|
    | Orchestrator | Claude (main session) | Manages state, routes, validates | N/A |
    | aegis-researcher | sonnet | Research follows instructions | haiku |
    | aegis-planner | inherit (opus) | Planning needs architecture reasoning | sonnet |
    | aegis-executor | sonnet | Follows explicit plan instructions | sonnet |
    | aegis-verifier | sonnet | Goal-backward reasoning needs mid-tier | haiku |
    | aegis-deployer | sonnet | Deploy actions need careful execution | sonnet |
    | GPT-4 Mini (Sparrow) | External/DeepSeek | Cheap autonomous sub-tasks only | Skip (graceful degradation) |

    **Create `references/invocation-protocol.md`:**

    A reference document defining the structured prompt template that the orchestrator MUST use for every Agent tool dispatch:

    ```
    ## Objective
    [One sentence: what the subagent must accomplish]

    ## Context Files (read these first)
    - [absolute path] -- [what it contains]

    ## Constraints
    - [Prior stage decisions that constrain this work]
    - [Naming conventions, file structure rules]

    ## Success Criteria
    - [Specific, verifiable condition 1]
    - [Specific, verifiable condition 2]

    ## Output
    - [What files to create/modify]
    - [Format of final message]
    ```

    Also document: (1) anti-patterns (vague prompts, implicit context assumptions, content dumping instead of file references), (2) GPT-4 Mini delegation via Sparrow section with invocation pattern and qualifying task types, (3) output format that subagents must return, (4) constraint that subagents cannot spawn sub-subagents.

    **Create `lib/aegis-validate.sh`:**

    Bash library (consistent with existing lib/ pattern) with these functions:

    ```bash
    validate_subagent_output(stage_name, expected_files...)
    # Checks each expected file exists. Returns 0 if all present, 1 with error on stderr if any missing.

    validate_sparrow_result(result_text)
    # Checks Sparrow response is non-empty and not an error pattern. Returns 0/1.
    ```

    Follow existing lib conventions: `set -euo pipefail`, use python3 for any JSON manipulation, configurable paths via environment variables for testing.
  </action>
  <verify>
    <automated>bash -c 'for f in .claude/agents/aegis-{researcher,planner,executor,verifier,deployer}.md references/model-routing.md references/invocation-protocol.md lib/aegis-validate.sh; do [ -f "$f" ] || { echo "MISSING: $f"; exit 1; }; done && echo "All 8 files exist"'</automated>
  </verify>
  <done>5 agent definition files exist with valid YAML frontmatter. Model routing table has 7-row routing table. Invocation protocol has structured prompt template. Validation library has validate_subagent_output and validate_sparrow_result functions.</done>
</task>

<task type="auto">
  <name>Task 2: Create test suite and update test runner</name>
  <files>
    tests/test-subagent-dispatch.sh
    tests/run-all.sh
  </files>
  <action>
    Create `tests/test-subagent-dispatch.sh` following the established test pattern from `tests/test-stage-workflows.sh`:
    - Use pass()/fail() helper functions
    - Tests run from PROJECT_ROOT

    **Tests to implement:**

    1. **test_agent_definitions_exist:** All 5 `.claude/agents/aegis-*.md` files exist.

    2. **test_agent_frontmatter_fields:** Each agent file contains required YAML frontmatter fields: `name`, `description`, `tools`, `model`, `permissionMode`, `maxTurns`. Use grep to check each field exists in each file.

    3. **test_agent_names_match_convention:** Each agent file's `name:` field matches `aegis-{role}` pattern. Extract the name field and verify it matches the filename.

    4. **test_model_routing_table_exists:** `references/model-routing.md` exists and contains the routing table with all 7 agent roles (grep for each: Orchestrator, aegis-researcher, aegis-planner, aegis-executor, aegis-verifier, aegis-deployer, "Sparrow").

    5. **test_invocation_protocol_sections:** `references/invocation-protocol.md` contains the 5 required prompt template sections: `## Objective`, `## Context Files`, `## Constraints`, `## Success Criteria`, `## Output`.

    6. **test_validation_library_functions:** `lib/aegis-validate.sh` is sourceable (`bash -n`) and contains `validate_subagent_output` and `validate_sparrow_result` function definitions.

    7. **test_validation_file_check:** Source `lib/aegis-validate.sh`, call `validate_subagent_output "test" "/tmp/nonexistent-file-$$"` and verify it returns 1 (fail). Then create a temp file and verify it returns 0 (pass).

    8. **test_sparrow_delegation_pattern:** `references/model-routing.md` documents Sparrow invocation pattern (grep for `/home/ai/scripts/sparrow`) and timeout handling (grep for `timeout`).

    **Update `tests/run-all.sh`:**
    Add `"test-subagent-dispatch"` to the TESTS array after `"test-advance-loop"`.
  </action>
  <verify>
    <automated>bash tests/test-subagent-dispatch.sh && bash tests/run-all.sh</automated>
  </verify>
  <done>test-subagent-dispatch.sh passes all 8 tests. run-all.sh includes the new test and full suite passes (10/10).</done>
</task>

</tasks>

<verification>
1. All 5 agent definition files in `.claude/agents/` have valid YAML frontmatter with name, description, tools, model, permissionMode, maxTurns
2. `references/model-routing.md` contains 7-row routing table with all agent roles
3. `references/invocation-protocol.md` contains structured prompt template with 5 sections
4. `lib/aegis-validate.sh` sources cleanly and has validate_subagent_output + validate_sparrow_result
5. `tests/test-subagent-dispatch.sh` passes all 8 tests
6. `bash tests/run-all.sh` reports 10/10 passed
</verification>

<success_criteria>
- 5 subagent definitions exist at `.claude/agents/aegis-{researcher,planner,executor,verifier,deployer}.md`
- Model routing table documents all agent roles with models, rationale, fallbacks
- Invocation protocol defines the mandatory structured prompt template
- Validation library provides file-existence and Sparrow-result validation
- Full test suite passes: `bash tests/run-all.sh` shows 10/10
</success_criteria>

<output>
After completion, create `.planning/phases/04-subagent-system/04-01-SUMMARY.md`
</output>
