---
phase: 04-subagent-system
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - workflows/pipeline/orchestrator.md
  - workflows/stages/02-research.md
  - workflows/stages/04-phase-plan.md
  - workflows/stages/05-execute.md
  - workflows/stages/06-verify.md
autonomous: true
requirements: [MDL-03, MDL-04]

must_haves:
  truths:
    - "Orchestrator Step 5 dispatches to subagents via Agent tool for research, phase-plan, execute, and verify stages"
    - "Orchestrator resolves model from routing table before each dispatch"
    - "Orchestrator validates subagent output using aegis-validate.sh before consuming results"
    - "Stage workflows include subagent dispatch instructions with structured invocation prompts"
    - "GPT-4 Mini delegation via Sparrow is documented as an option in relevant stage workflows"
    - "Non-subagent stages (intake, roadmap, test-gate, advance, deploy) are unchanged"
  artifacts:
    - path: "workflows/pipeline/orchestrator.md"
      provides: "Updated Step 5 with subagent dispatch logic"
      contains: "Agent tool"
    - path: "workflows/stages/02-research.md"
      provides: "Research stage with subagent dispatch"
      contains: "aegis-researcher"
    - path: "workflows/stages/04-phase-plan.md"
      provides: "Phase-plan stage with subagent dispatch"
      contains: "aegis-planner"
    - path: "workflows/stages/05-execute.md"
      provides: "Execute stage with subagent dispatch"
      contains: "aegis-executor"
    - path: "workflows/stages/06-verify.md"
      provides: "Verify stage with subagent dispatch"
      contains: "aegis-verifier"
  key_links:
    - from: "workflows/pipeline/orchestrator.md"
      to: "references/model-routing.md"
      via: "Step 5 references routing table for model selection"
      pattern: "model-routing"
    - from: "workflows/pipeline/orchestrator.md"
      to: "references/invocation-protocol.md"
      via: "Step 5 references invocation protocol for prompt construction"
      pattern: "invocation-protocol"
    - from: "workflows/pipeline/orchestrator.md"
      to: "lib/aegis-validate.sh"
      via: "Step 5 sources validation library for output checks"
      pattern: "aegis-validate"
    - from: "workflows/stages/02-research.md"
      to: ".claude/agents/aegis-researcher.md"
      via: "Stage dispatches to aegis-researcher subagent"
      pattern: "aegis-researcher"
---

<objective>
Wire subagent dispatch into the orchestrator and 4 GSD-delegating stage workflows so the pipeline actually uses the subagent system.

Purpose: Connects the foundation artifacts from Plan 01 into the live pipeline, making the orchestrator a lean dispatcher that delegates heavy work to subagents with fresh context.
Output: Updated orchestrator.md with subagent Step 5, updated stage workflows for research/phase-plan/execute/verify.
</objective>

<execution_context>
@/home/ai/.claude/get-shit-done/workflows/execute-plan.md
@/home/ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-subagent-system/04-RESEARCH.md
@.planning/phases/04-subagent-system/04-01-SUMMARY.md
@workflows/pipeline/orchestrator.md
@workflows/stages/02-research.md
@workflows/stages/05-execute.md
@workflows/stages/06-verify.md
@references/model-routing.md
@references/invocation-protocol.md
@lib/aegis-validate.sh

<interfaces>
<!-- From Plan 01 — agent definitions, routing, validation -->
From .claude/agents/aegis-*.md:
```yaml
name: aegis-{researcher|planner|executor|verifier|deployer}
model: {sonnet|inherit|sonnet|sonnet|sonnet}
```

From references/model-routing.md:
```
| Agent Role | Model | Rationale | Fallback |
# 7-row routing table
```

From references/invocation-protocol.md:
```
## Objective / ## Context Files / ## Constraints / ## Success Criteria / ## Output
```

From lib/aegis-validate.sh:
```bash
validate_subagent_output(stage_name, expected_files...)
validate_sparrow_result(result_text)
```

From lib/aegis-detect.sh:
```bash
detect_integrations()  # Returns JSON with sparrow.available
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update orchestrator Step 5 with subagent dispatch logic</name>
  <files>
    workflows/pipeline/orchestrator.md
  </files>
  <action>
    Read `workflows/pipeline/orchestrator.md` and update Step 5 to dispatch via subagents for the 4 GSD-delegating stages.

    **Replace the current Step 5** with a two-path dispatch:

    **Path A — Subagent stages (research, phase-plan, execute, verify):**
    1. Read the stage workflow file to extract inputs, outputs, and success criteria
    2. Resolve the agent name from stage-to-agent mapping table:
       | Stage | Agent | Agent File |
       |-------|-------|------------|
       | research | aegis-researcher | .claude/agents/aegis-researcher.md |
       | phase-plan | aegis-planner | .claude/agents/aegis-planner.md |
       | execute | aegis-executor | .claude/agents/aegis-executor.md |
       | verify | aegis-verifier | .claude/agents/aegis-verifier.md |
    3. Resolve the model from `references/model-routing.md`
    4. Build the invocation prompt using the template from `references/invocation-protocol.md`, filling in:
       - Objective: from stage workflow
       - Context Files: `.aegis/state.current.json`, `.planning/ROADMAP.md`, plus stage-specific inputs
       - Constraints: any prior-stage decisions from state
       - Success Criteria: from stage workflow's "Completion Criteria"
       - Output: from stage workflow's "Outputs"
    5. Dispatch via Agent tool with the constructed prompt
    6. On return: source `lib/aegis-validate.sh` and call `validate_subagent_output "$CURRENT_STAGE" {expected_files}`
    7. If validation passes: fall through to Step 5.5 (gate evaluation)
    8. If validation fails: log failure, mark stage as failed, STOP

    **Path B — Non-subagent stages (intake, roadmap, test-gate, advance, deploy):**
    Keep current behavior: follow the stage workflow file directly (inline execution).

    **Also add to Libraries section at top:** `lib/aegis-validate.sh` -- Output validation for subagent results.

    **Also add to Important Rules:** Rule 6: "For subagent stages, use the Agent tool with a structured prompt from invocation-protocol.md. NEVER follow the stage workflow inline for subagent stages -- the subagent handles it in fresh context."

    **Keep all other Steps (1, 2, 3, 4, 5.5, 6) unchanged.** Only modify Step 5 and add the new library reference and rule.

    **Add a new entry to the Handled Scenarios table:**
    | Subagent dispatch | Build structured prompt from invocation protocol, dispatch via Agent tool, validate output |
    | Subagent validation fails | Log error, mark stage failed, stop pipeline |
    | GPT-4 Mini delegation | Stage workflow optionally delegates cheap tasks via Sparrow (see model-routing.md) |
  </action>
  <verify>
    <automated>bash -c 'grep -q "Agent tool" workflows/pipeline/orchestrator.md && grep -q "aegis-validate" workflows/pipeline/orchestrator.md && grep -q "invocation-protocol" workflows/pipeline/orchestrator.md && grep -q "model-routing" workflows/pipeline/orchestrator.md && echo "Orchestrator updated with subagent dispatch"'</automated>
  </verify>
  <done>Orchestrator Step 5 has two-path dispatch (subagent vs inline). References model-routing.md, invocation-protocol.md, and aegis-validate.sh. Non-subagent stages unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update 4 GSD-delegating stage workflows with subagent instructions</name>
  <files>
    workflows/stages/02-research.md
    workflows/stages/04-phase-plan.md
    workflows/stages/05-execute.md
    workflows/stages/06-verify.md
  </files>
  <action>
    Update the 4 GSD-delegating stage workflows to include subagent-aware instructions. These workflows are now executed BY the subagent (in fresh context), not by the orchestrator inline.

    **For each of the 4 files, add a new section `## Subagent Context` at the top (after the `# Stage:` title) containing:**

    ```markdown
    ## Subagent Context

    This stage is executed by the `aegis-{role}` subagent, dispatched by the orchestrator via Agent tool.
    The subagent receives a structured prompt with Objective, Context Files, Constraints, Success Criteria, and Output.

    **Agent:** aegis-{role}
    **Model:** {from routing table}
    **Invocation:** Orchestrator builds prompt per `references/invocation-protocol.md`

    **GPT-4 Mini delegation:** {stage-specific guidance on whether cheap sub-tasks can be delegated to Sparrow}
    ```

    **Stage-specific GPT-4 Mini guidance:**
    - **02-research.md:** "Sparrow can summarize long documents or format findings, but NOT evaluate technical decisions. Use `/home/ai/scripts/sparrow 'summarize: ...' --timeout 60` with graceful fallback."
    - **04-phase-plan.md:** "Do NOT delegate planning to Sparrow. Planning requires architecture reasoning. All work stays in this subagent."
    - **05-execute.md:** "Sparrow can generate boilerplate or format outputs, but NOT write production logic. Use sparingly."
    - **06-verify.md:** "Sparrow can format verification reports, but NOT evaluate pass/fail criteria. Judgment stays in this subagent."

    **Keep all existing sections** (## Inputs, ## Actions, ## Outputs, ## Completion Criteria) unchanged. The Subagent Context section is additive.

    **Also add to each file's ## Completion Criteria:**
    ```
    - Return structured completion message to orchestrator:
      - Files created/modified: [list]
      - Success criteria met: [yes/no for each]
      - Issues encountered: [list or none]
    ```

    **Do NOT modify the 5 non-subagent stage workflows** (01-intake, 03-roadmap, 07-test-gate, 08-advance, 09-deploy).
  </action>
  <verify>
    <automated>bash -c 'for stage in 02-research 04-phase-plan 05-execute 06-verify; do grep -q "Subagent Context" "workflows/stages/${stage}.md" || { echo "MISSING subagent context in ${stage}"; exit 1; }; grep -q "aegis-" "workflows/stages/${stage}.md" || { echo "MISSING agent reference in ${stage}"; exit 1; }; done && for stage in 01-intake 03-roadmap 07-test-gate 08-advance 09-deploy; do grep -q "Subagent Context" "workflows/stages/${stage}.md" 2>/dev/null && { echo "UNEXPECTED subagent context in ${stage}"; exit 1; }; done; echo "Stage workflows correctly updated" && bash tests/test-stage-workflows.sh && bash tests/run-all.sh'</automated>
  </verify>
  <done>4 GSD-delegating stages have Subagent Context sections with agent name, model, GPT-4 Mini guidance, and structured completion message format. 5 non-subagent stages remain unchanged. All existing tests still pass.</done>
</task>

</tasks>

<verification>
1. Orchestrator Step 5 has two-path dispatch: subagent (research, phase-plan, execute, verify) and inline (intake, roadmap, test-gate, advance, deploy)
2. Orchestrator references model-routing.md, invocation-protocol.md, and aegis-validate.sh
3. 4 stage workflows have Subagent Context sections with correct agent names and GPT-4 Mini guidance
4. 5 non-subagent stage workflows are unchanged
5. `bash tests/test-stage-workflows.sh` passes (existing tests still green)
6. `bash tests/run-all.sh` passes (full suite including new subagent tests)
</verification>

<success_criteria>
- Orchestrator dispatches subagents for 4 stages with structured invocation prompts
- Orchestrator validates subagent output before consuming
- Stage workflows document their subagent role and GPT-4 Mini delegation rules
- Full test suite passes: `bash tests/run-all.sh` shows 10/10
</success_criteria>

<output>
After completion, create `.planning/phases/04-subagent-system/04-02-SUMMARY.md`
</output>
