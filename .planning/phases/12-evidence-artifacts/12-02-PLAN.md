---
phase: 12-evidence-artifacts
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - lib/aegis-gates.sh
  - tests/test-gate-evaluation.sh
  - tests/test-state-transitions.sh
  - tests/test-journaled-state.sh
  - tests/test-integration-detection.sh
  - tests/test-memory-stub.sh
  - tests/test-memory-engram.sh
  - tests/test-memory-scoping.sh
  - tests/test-memory-migration.sh
  - tests/test-gate-banners.sh
  - tests/test-git-operations.sh
  - tests/test-stage-workflows.sh
  - tests/test-advance-loop.sh
  - tests/test-subagent-dispatch.sh
  - tests/test-consultation.sh
  - tests/test-policy-config.sh
  - tests/test-complete-stage.sh
  - tests/test-namespace.sh
  - tests/test-checkpoints.sh
  - tests/test-behavioral-gate.sh
  - tests/test-preflight.sh
  - tests/test-pipeline-integration.sh
autonomous: true
requirements: [EVID-02, EVID-03]

must_haves:
  truths:
    - "evaluate_gate accepts a phase parameter and calls validate_evidence before gate logic"
    - "evaluate_gate returns 'evidence-missing' when no evidence artifact exists for the stage+phase"
    - "evaluate_gate returns 'evidence-invalid' when evidence artifact fails validation"
    - "evaluate_gate proceeds normally when evidence is valid"
    - "evaluate_gate skips evidence check when phase is 0 or unset (backward compatibility)"
    - "All existing test names include [REQ-ID] prefix so test-gate requirement enforcement passes"
    - "Full test suite passes with zero regressions after all changes"
  artifacts:
    - path: "lib/aegis-gates.sh"
      provides: "Evidence pre-check in evaluate_gate"
      contains: "validate_evidence"
    - path: "tests/test-gate-evaluation.sh"
      provides: "Tests for evidence-aware gate evaluation"
      contains: "evidence-missing"
  key_links:
    - from: "lib/aegis-gates.sh"
      to: "lib/aegis-evidence.sh"
      via: "source + validate_evidence call"
      pattern: "source.*aegis-evidence"
    - from: "lib/aegis-gates.sh"
      to: ".aegis/evidence/"
      via: "validate_evidence reads evidence files"
      pattern: "validate_evidence"
---

<objective>
Wire evidence validation into gate evaluation and migrate all existing test names to include [REQ-ID] prefixes for EVID-03 compliance.

Purpose: EVID-02 requires gates to programmatically check evidence artifacts. EVID-03 requires test output to contain requirement references. Without gate integration, evidence artifacts exist but are not enforced. Without [REQ-ID] prefixes, the test-gate would reject the existing test suite.

Output: Modified `lib/aegis-gates.sh` with evidence pre-check, all 21 test files with [REQ-ID]-prefixed test names, full suite green.
</objective>

<execution_context>
@/home/ai/.claude/get-shit-done/workflows/execute-plan.md
@/home/ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-evidence-artifacts/12-RESEARCH.md
@.planning/phases/12-evidence-artifacts/12-01-SUMMARY.md

<interfaces>
<!-- From lib/aegis-evidence.sh (created in Plan 01): -->
```bash
write_evidence(stage, phase, files_json, requirements_json, stage_specific_json, checks_json)
  # Creates .aegis/evidence/{stage}-phase-{phase}.json with hashes and policy version
  # Returns: path to evidence file

validate_evidence(stage, phase)
  # Returns: "valid" (exit 0), "missing" (exit 1), "invalid" (exit 1)

query_evidence(requirement_id)
  # Returns: JSON array of matching evidence, or "not-found" (exit 1)

validate_test_requirements(test_output)
  # Returns: JSON array of requirement IDs, or rejects (exit 1)
```

<!-- Current evaluate_gate signature (from lib/aegis-gates.sh): -->
```bash
evaluate_gate(stage_name, yolo_mode)
# Returns: pass | fail | approval-needed | auto-approved
# Must add: phase as 3rd param, evidence pre-check
# Must add return values: evidence-missing | evidence-invalid
```

<!-- Backward compatibility requirement from research Pitfall 5: -->
<!-- Make evidence pre-check conditional on phase: if phase is 0 or not provided, skip -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add evidence pre-check to evaluate_gate and update gate tests</name>
  <files>lib/aegis-gates.sh, tests/test-gate-evaluation.sh</files>
  <action>
Modify `evaluate_gate()` in `lib/aegis-gates.sh`:

1. Add `phase` as optional 3rd parameter: `local phase="${3:-0}"`
2. Add `source "$AEGIS_LIB_DIR/aegis-evidence.sh"` at the top of the file (alongside existing sources)
3. Before the existing python3 gate evaluation block, add evidence pre-check:
   - If phase > 0: call `validate_evidence "$stage_name" "$phase"` and capture result
   - If result is "missing": echo "evidence-missing" and return 0
   - If result is "invalid": echo "evidence-invalid" and return 0
   - If phase is 0 or unset: skip evidence check entirely (backward compatibility)
4. Existing gate logic remains unchanged after the pre-check

Update `tests/test-gate-evaluation.sh`:
- Add 3 new tests for evidence-aware gate evaluation:
  - `[EVID-02] evaluate_gate returns evidence-missing when no evidence file exists` — call evaluate_gate with phase=1, no evidence file
  - `[EVID-02] evaluate_gate returns evidence-invalid when evidence has missing fields` — write a malformed evidence JSON, call evaluate_gate
  - `[EVID-02] evaluate_gate passes with valid evidence` — call write_evidence to create proper evidence, then call evaluate_gate
- For the evidence-valid test: create a minimal evidence artifact using write_evidence from lib/aegis-evidence.sh
- Existing tests continue to work because they call evaluate_gate with only 2 args (phase defaults to 0, evidence check skipped)

Commit: `feat(12-02): add evidence pre-check to evaluate_gate [EVID-02]`
  </action>
  <verify>
    <automated>cd /home/ai/aegis && bash tests/test-gate-evaluation.sh && bash tests/test-evidence.sh</automated>
  </verify>
  <done>evaluate_gate returns evidence-missing/evidence-invalid for phase>0 without evidence. Existing gate tests pass unchanged (backward compatible). 3 new evidence gate tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all existing test names to [REQ-ID] prefix convention</name>
  <files>tests/test-state-transitions.sh, tests/test-journaled-state.sh, tests/test-integration-detection.sh, tests/test-memory-stub.sh, tests/test-memory-engram.sh, tests/test-memory-scoping.sh, tests/test-memory-migration.sh, tests/test-gate-evaluation.sh, tests/test-gate-banners.sh, tests/test-git-operations.sh, tests/test-stage-workflows.sh, tests/test-advance-loop.sh, tests/test-subagent-dispatch.sh, tests/test-consultation.sh, tests/test-policy-config.sh, tests/test-complete-stage.sh, tests/test-namespace.sh, tests/test-checkpoints.sh, tests/test-behavioral-gate.sh, tests/test-preflight.sh, tests/test-pipeline-integration.sh</files>
  <action>
For EVID-03 compliance, every `pass "..."` and `fail "..."` call in existing test files must include a `[REQ-ID]` prefix. This is mechanical: prefix each test name string with the requirement ID it validates.

Requirement ID mapping by test file (reference REQUIREMENTS.md):
- test-state-transitions.sh: [PIPE-02], [PIPE-07]
- test-journaled-state.sh: [PIPE-07]
- test-integration-detection.sh: [PORT-01]
- test-memory-stub.sh: [MEM-01]
- test-memory-engram.sh: [MEM-01], [MEM-02]
- test-memory-scoping.sh: [MEM-04], [MEM-08]
- test-memory-migration.sh: [MEM-05]
- test-gate-evaluation.sh: [PIPE-03], [PIPE-06], [EVID-02] (for evidence tests added in Task 1)
- test-gate-banners.sh: [PIPE-04]
- test-git-operations.sh: [GIT-01], [GIT-02], [GIT-03]
- test-stage-workflows.sh: [PIPE-02]
- test-advance-loop.sh: [PIPE-02]
- test-subagent-dispatch.sh: [MDL-03], [MDL-04]
- test-consultation.sh: [MDL-01], [MDL-02]
- test-policy-config.sh: [POLC-01], [POLC-02]
- test-complete-stage.sh: [FOUND-01]
- test-namespace.sh: [FOUND-02]
- test-checkpoints.sh: [CHKP-01], [CHKP-02], [CHKP-03]
- test-behavioral-gate.sh: [AGENT-01], [AGENT-02]
- test-preflight.sh: [DEPLOY-01], [DEPLOY-02], [DEPLOY-03]
- test-pipeline-integration.sh: [PIPE-02], [PIPE-07]

For each file: Read the file, identify every `pass "..."` and `fail "..."` call, prefix the test name string with the appropriate `[REQ-ID]` based on which requirement the test validates. When a test clearly validates a specific requirement, use that ID. When ambiguous, use the primary requirement for that test file.

Example transformation:
  Before: `pass "load_policy succeeds with valid config"`
  After:  `pass "[POLC-01] load_policy succeeds with valid config"`

  Before: `fail "evaluate_gate returns pass for completed stage" "$result"`
  After:  `fail "[PIPE-03] evaluate_gate returns pass for completed stage" "$result"`

This is a bulk mechanical edit across 21 files. No logic changes -- only string prefixes on test names. Run full test suite after to confirm zero regressions.

Commit: `refactor(12-02): add [REQ-ID] prefix to all test names for EVID-03 compliance`
  </action>
  <verify>
    <automated>cd /home/ai/aegis && bash tests/run-all.sh && bash tests/run-all.sh 2>&1 | grep -c "PASS:" | xargs -I {} test {} -gt 0</automated>
  </verify>
  <done>All 21+ test files pass. Every pass/fail call includes a [REQ-ID] prefix. validate_test_requirements would accept the test output (contains requirement ID references, non-empty suite).</done>
</task>

</tasks>

<verification>
- `bash tests/run-all.sh` — full suite passes (22 test files, 0 failures)
- `bash tests/test-gate-evaluation.sh` — evidence pre-check tests pass
- `grep -r 'pass "' tests/ | grep -v '\[' | wc -l` returns 0 (no un-prefixed test names)
- evaluate_gate with phase>0 requires evidence; phase=0 skips (backward compat)
</verification>

<success_criteria>
- evaluate_gate accepts optional phase parameter (3rd arg)
- Gate returns evidence-missing/evidence-invalid when evidence is absent or malformed
- Gate passes normally when evidence is valid
- Backward compatibility: existing 2-arg calls work unchanged
- All 21 existing test files have [REQ-ID] prefixes on every pass/fail call
- Full test suite (22 files) passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/12-evidence-artifacts/12-02-SUMMARY.md`
</output>
