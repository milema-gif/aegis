---
phase: 06-multi-model-consultation
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - workflows/pipeline/orchestrator.md
  - templates/pipeline-state.json
autonomous: true
requirements:
  - MDL-01
  - MDL-02

must_haves:
  truths:
    - "Orchestrator runs consultation at Step 5.55 between gate evaluation and memory persistence"
    - "Routine gates (research, roadmap, phase-plan) send context to DeepSeek via Sparrow"
    - "Critical gates (verify, deploy) send to Codex ONLY when user explicitly opted in"
    - "Critical gates fall back to DeepSeek when codex not opted in"
    - "Consultation failure (Sparrow down, timeout) skips review and continues pipeline"
    - "Consultation results are displayed to user via banner, never silently consumed"
    - "Codex opt-in is detected from launch arguments and stored in state config"
  artifacts:
    - path: "workflows/pipeline/orchestrator.md"
      provides: "Step 5.55 (consultation) and codex opt-in in Step 1"
      contains: "Step 5.55"
    - path: "templates/pipeline-state.json"
      provides: "codex_opted_in field in config"
      contains: "codex_opted_in"
  key_links:
    - from: "workflows/pipeline/orchestrator.md"
      to: "lib/aegis-consult.sh"
      via: "source lib/aegis-consult.sh at Step 5.55"
      pattern: "aegis-consult"
    - from: "workflows/pipeline/orchestrator.md"
      to: "references/consultation-config.md"
      via: "get_consultation_type reads config"
      pattern: "consultation_type"
    - from: "workflows/pipeline/orchestrator.md"
      to: "state.current.json"
      via: "codex_opted_in field in config"
      pattern: "codex_opted_in"
---

<objective>
Wire the consultation library into the pipeline orchestrator: add Step 5.55 (consultation between gate eval and memory persist), add codex opt-in detection at Step 1, and update the state template.

Purpose: This completes MDL-01 and MDL-02 by connecting the consultation building blocks (from Plan 01) into the live pipeline flow. After this, routine gates consult DeepSeek and critical gates consult Codex (when user opts in).

Output: Updated `workflows/pipeline/orchestrator.md` with consultation step, updated `templates/pipeline-state.json` with codex_opted_in field
</objective>

<execution_context>
@/home/ai/.claude/get-shit-done/workflows/execute-plan.md
@/home/ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-multi-model-consultation/06-RESEARCH.md
@.planning/phases/06-multi-model-consultation/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — the consultation library functions -->
From lib/aegis-consult.sh (created in Plan 01):
```bash
get_consultation_type(stage_name)                    # Returns "none" | "routine" | "critical"
consult_sparrow(message, use_codex, timeout_secs)    # Returns result string or empty
build_consultation_context(stage, project)            # Returns structured review prompt
show_consultation_banner(model, stage, result)        # Displays formatted banner
read_codex_opt_in()                                  # Returns "true" | "false"
set_codex_opt_in(value)                              # Writes to state config
```

From orchestrator.md (insertion points):
```
Step 1 — Resolve Project        # Add codex opt-in detection here
Step 5.5 — Evaluate Gate        # Consultation goes AFTER this
Step 5.6 — Persist Gate Memory  # Consultation goes BEFORE this
```

From templates/pipeline-state.json (config section):
```json
"config": {
  "auto_advance": false,
  "yolo_mode": false
  // Add: "codex_opted_in": false
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add consultation step and codex opt-in to orchestrator</name>
  <files>workflows/pipeline/orchestrator.md</files>
  <action>
Read the current `workflows/pipeline/orchestrator.md` and make these additions:

**1. Update Libraries section** — Add `lib/aegis-consult.sh` to the list of foundation libraries:
- `**lib/aegis-consult.sh** -- Consultation: send context to external models (DeepSeek/Codex) at configurable gates`

**2. Update Step 1 (Resolve Project)** — After resolving the project name, add codex opt-in detection:
```
### Codex Opt-In Detection

After resolving the project name, check if the user said "codex" in their launch arguments:

source lib/aegis-consult.sh

CODEX_OPTED_IN="false"
if echo "$ARGUMENTS" | grep -qi "codex"; then
  CODEX_OPTED_IN="true"
fi
set_codex_opt_in "$CODEX_OPTED_IN"
```

This sets `config.codex_opted_in` in state. It defaults to `false`. Only set to `true` when the user literally includes "codex" in their `/aegis:launch` arguments. This is a hard rule from CLAUDE.md — Codex is NEVER auto-invoked.

**3. Add Step 5.55 — External Model Consultation** — Insert between Step 5.5 (Evaluate Gate) and Step 5.6 (Persist Gate Memory). The step runs ONLY when the gate result is "pass" or "auto-approved" (same condition as Step 5.6):

```
## Step 5.55 -- External Model Consultation

After gate passes (Step 5.5 result is "pass" or "auto-approved"), optionally consult an external model for review feedback. Consultation is ADVISORY — it never blocks the pipeline.

source lib/aegis-consult.sh

CURRENT_STAGE=$(read_current_stage)
CONSULT_TYPE=$(get_consultation_type "$CURRENT_STAGE")

if [[ "$CONSULT_TYPE" == "none" ]]; then
  # No consultation for this stage, continue to Step 5.6
  :
elif [[ "$CONSULT_TYPE" == "routine" ]]; then
  CONTEXT=$(build_consultation_context "$CURRENT_STAGE" "$PROJECT_NAME")
  RESULT=$(consult_sparrow "$CONTEXT" "false" 60)
  if [[ -n "$RESULT" ]]; then
    show_consultation_banner "DeepSeek" "$CURRENT_STAGE" "$RESULT"
  else
    echo "[consultation] Sparrow unavailable, skipping routine review."
  fi
elif [[ "$CONSULT_TYPE" == "critical" ]]; then
  CODEX_OPT_IN=$(read_codex_opt_in)
  if [[ "$CODEX_OPT_IN" == "true" ]]; then
    CONTEXT=$(build_consultation_context "$CURRENT_STAGE" "$PROJECT_NAME")
    RESULT=$(consult_sparrow "$CONTEXT" "true" 120)
    if [[ -n "$RESULT" ]]; then
      show_consultation_banner "GPT Codex" "$CURRENT_STAGE" "$RESULT"
    else
      echo "[consultation] Codex unavailable, skipping critical review."
    fi
  else
    # Fall back to DeepSeek for critical stages when codex not opted-in
    CONTEXT=$(build_consultation_context "$CURRENT_STAGE" "$PROJECT_NAME")
    RESULT=$(consult_sparrow "$CONTEXT" "false" 60)
    if [[ -n "$RESULT" ]]; then
      show_consultation_banner "DeepSeek" "$CURRENT_STAGE" "$RESULT"
    else
      echo "[consultation] Sparrow unavailable, skipping review."
    fi
  fi
fi

# Continue to Step 5.6 regardless of consultation outcome
```

**4. Update Step 5.5 flow** — In the "Advance (gate passed)" section (point 4), update to reference Step 5.55 before Step 5.6:
- "Proceed to **Step 5.55** (External Model Consultation), then **Step 5.6** (Persist Gate Memory) before advancing."

**5. Update Handled Scenarios table** — Add these rows:
| Routine consultation | At configurable gates (research, roadmap, phase-plan), context sent to DeepSeek via Sparrow. Result displayed as banner. |
| Critical consultation (codex opted-in) | At verify/deploy, context sent to Codex via Sparrow --codex. Result displayed as banner. |
| Critical consultation (codex not opted-in) | Falls back to DeepSeek. Codex NEVER auto-invoked. |
| Consultation failure | Sparrow unavailable or timeout: skip review, log warning, continue pipeline. Never blocks. |
| Codex opt-in detection | Checked at Step 1 from launch arguments. Stored in state config. Default false. |

IMPORTANT: Do NOT modify any existing steps (5.5 gate evaluation, 5.6 memory persistence, etc.) beyond the targeted additions above. The orchestrator must remain backward-compatible.
  </action>
  <verify>
    <automated>grep -q "Step 5.55" /home/ai/aegis/workflows/pipeline/orchestrator.md && grep -q "aegis-consult" /home/ai/aegis/workflows/pipeline/orchestrator.md && grep -q "codex_opted_in" /home/ai/aegis/workflows/pipeline/orchestrator.md && grep -q "CONSULTATION" /home/ai/aegis/workflows/pipeline/orchestrator.md && echo "All orchestrator checks passed"</automated>
  </verify>
  <done>
    - Step 5.55 exists between Step 5.5 and Step 5.6 in orchestrator.md
    - Codex opt-in detection added to Step 1
    - lib/aegis-consult.sh listed in Libraries section
    - Handled Scenarios table includes consultation rows
    - Codex is NEVER invoked without explicit user opt-in (grep confirms conditional)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update state template and run full test suite</name>
  <files>templates/pipeline-state.json</files>
  <action>
Read `templates/pipeline-state.json` and add `"codex_opted_in": false` to the `config` object (alongside existing `auto_advance` and `yolo_mode` fields).

Then run the full test suite to verify no regressions:
```bash
bash tests/run-all.sh
```

All tests must pass, including the new `test-consultation` added in Plan 01.

Also verify the orchestrator update didn't break any structural expectations by checking:
- The orchestrator still has all original steps (1 through 6)
- Step 5.5 (gate evaluation) is intact
- Step 5.6 (persist gate memory) is intact
- The new Step 5.55 is between them
  </action>
  <verify>
    <automated>grep -q "codex_opted_in" /home/ai/aegis/templates/pipeline-state.json && bash /home/ai/aegis/tests/run-all.sh && echo "State template updated and all tests pass"</automated>
  </verify>
  <done>
    - templates/pipeline-state.json has codex_opted_in: false in config
    - bash tests/run-all.sh passes all tests (including test-consultation)
    - No regressions in existing test suite
  </done>
</task>

</tasks>

<verification>
1. `grep "Step 5.55" workflows/pipeline/orchestrator.md` — consultation step exists
2. `grep "codex_opted_in" templates/pipeline-state.json` — state template updated
3. `grep "aegis-consult" workflows/pipeline/orchestrator.md` — library referenced
4. `bash tests/run-all.sh` — full suite passes
5. Orchestrator steps 1-6 all present and in order
</verification>

<success_criteria>
- Orchestrator has Step 5.55 between 5.5 and 5.6 that dispatches to consultation library
- Codex opt-in detected at Step 1 from user arguments, stored in state config
- Critical gates check codex_opted_in before using --codex flag
- State template includes codex_opted_in: false default
- Full test suite passes with zero regressions
- Pipeline handles Sparrow unavailability gracefully (skip and continue)
</success_criteria>

<output>
After completion, create `.planning/phases/06-multi-model-consultation/06-02-SUMMARY.md`
</output>
