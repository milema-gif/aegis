---
phase: 16-patterns-and-rollback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/aegis-patterns.sh
  - lib/aegis-rollback-drill.sh
  - tests/test-patterns.sh
  - tests/test-rollback-drill.sh
autonomous: true
requirements: [PATN-01, PATN-03, ROLL-01]

must_haves:
  truths:
    - "Operator can save a pattern with name, project origin, description, and pattern text"
    - "Saved patterns default to unapproved (draft) state"
    - "Operator can approve a pattern by ID, setting approved=true with timestamp"
    - "Operator can list and retrieve patterns from the library"
    - "Rollback drill verifies recovery capability by checking out prior phase tag"
    - "Rollback drill cleans up temp branch on success or failure"
    - "Rollback drill skips gracefully when no prior tag exists"
  artifacts:
    - path: "lib/aegis-patterns.sh"
      provides: "Pattern CRUD operations"
      exports: ["save_pattern", "approve_pattern", "list_patterns", "get_pattern"]
    - path: "lib/aegis-rollback-drill.sh"
      provides: "Rollback drill execution"
      exports: ["run_rollback_drill"]
    - path: "tests/test-patterns.sh"
      provides: "Pattern library test coverage"
      contains: "PATN-01"
    - path: "tests/test-rollback-drill.sh"
      provides: "Rollback drill test coverage"
      contains: "ROLL-01"
  key_links:
    - from: "lib/aegis-rollback-drill.sh"
      to: "lib/aegis-git.sh"
      via: "source for check_rollback_compatibility, tag functions"
      pattern: "source.*aegis-git.sh"
    - from: "lib/aegis-rollback-drill.sh"
      to: "lib/aegis-evidence.sh"
      via: "source for write_evidence (drill results)"
      pattern: "source.*aegis-evidence.sh"
    - from: "lib/aegis-patterns.sh"
      to: ".aegis/patterns/"
      via: "JSON file storage"
      pattern: "patterns_dir.*patterns"
---

<objective>
Create the pattern library and rollback drill library with full test coverage.

Purpose: PATN-01/PATN-03 require an opt-in pattern library with operator approval gating. ROLL-01 requires a deterministic rollback drill that proves recovery works. Both are new library files following the established aegis bash+python3 pattern.
Output: Two new library files (`aegis-patterns.sh`, `aegis-rollback-drill.sh`) and two test files.
</objective>

<execution_context>
@/home/ai/.claude/get-shit-done/workflows/execute-plan.md
@/home/ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-patterns-and-rollback/16-RESEARCH.md

<interfaces>
<!-- Existing libraries the new code sources/uses -->

From lib/aegis-evidence.sh:
```bash
write_evidence(stage, phase, files_json, requirements_json, stage_specific_json, checks_json)
# Creates .aegis/evidence/{stage}-phase-{phase}.json
# Returns path to evidence file on stdout
```

From lib/aegis-git.sh:
```bash
tag_phase_completion(phase_number, phase_name)
list_phase_tags()
check_rollback_compatibility(tag)  # Returns "compatible" or warning text
rollback_to_tag(tag)
```

From lib/aegis-policy.sh:
```bash
get_policy_version()  # Returns policy version string
```

Test pattern (from tests/test-regression.sh):
```bash
pass() { echo "PASS: $1"; PASS_COUNT=$((PASS_COUNT + 1)); }
fail() { echo "FAIL: $1 — $2"; FAIL_COUNT=$((FAIL_COUNT + 1)); }
setup() { TEST_DIR=$(mktemp -d); export AEGIS_DIR="$TEST_DIR/.aegis"; ... }
teardown() { [[ -n "${TEST_DIR:-}" ]] && rm -rf "$TEST_DIR"; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pattern library with tests (PATN-01, PATN-03)</name>
  <files>lib/aegis-patterns.sh, tests/test-patterns.sh</files>
  <behavior>
    - [PATN-01] save_pattern creates JSON file in .aegis/patterns/ with correct schema (id, name, project_origin, description, pattern, tags, created_at, approved, approved_at, approved_by)
    - [PATN-01] list_patterns returns JSON array of all patterns
    - [PATN-01] get_pattern retrieves a pattern by ID, returns JSON
    - [PATN-01] get_pattern returns error JSON for nonexistent ID
    - [PATN-03] save_pattern defaults approved=false, approved_at=null, approved_by=null
    - [PATN-03] approve_pattern sets approved=true, approved_at to current timestamp, approved_by to "operator"
    - [PATN-03] approve_pattern returns error for nonexistent pattern ID
    - [PATN-01] save_pattern rejects duplicate pattern IDs (returns error, does not overwrite)
    - [PATN-01] Pattern ID is slugified from name (lowercase, spaces to hyphens, alphanumeric+hyphens only)
  </behavior>
  <action>
    Write tests first in `tests/test-patterns.sh` following the exact test pattern from `tests/test-regression.sh`:
    - Use `pass()/fail()` helpers, `setup()/teardown()` with temp dir, `AEGIS_DIR` env var
    - Prefix each test assertion with `[PATN-01]` or `[PATN-03]` per requirement traceability convention
    - Tests create temp dirs, call library functions, verify JSON output with python3

    Then create `lib/aegis-patterns.sh`:
    - Follow `aegis-evidence.sh` conventions: `set -euo pipefail`, `AEGIS_LIB_DIR` detection, python3 for JSON ops, atomic tmp+mv writes
    - Functions: `save_pattern(name, project_origin, description, pattern_text, tags_json)`, `approve_pattern(pattern_id)`, `list_patterns()`, `get_pattern(pattern_id)`
    - Storage: `.aegis/patterns/{slug-id}.json` as individual files
    - Slug generation: `echo "$name" | tr '[:upper:]' '[:lower:]' | tr ' ' '-' | tr -cd 'a-z0-9-'`
    - Collision detection: if file exists, return error (don't overwrite)
    - Schema version: "1.0.0"
    - Use the code examples from 16-RESEARCH.md as starting point but ensure all edge cases are handled

    Run tests -- RED then GREEN.
  </action>
  <verify>
    <automated>bash tests/test-patterns.sh</automated>
  </verify>
  <done>All pattern library tests pass. save_pattern creates correct JSON, list/get retrieve it, approve flips the flag, duplicates rejected, all [PATN-01] and [PATN-03] assertions green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rollback drill library with tests (ROLL-01)</name>
  <files>lib/aegis-rollback-drill.sh, tests/test-rollback-drill.sh</files>
  <behavior>
    - [ROLL-01] run_rollback_drill with valid prior tag creates temp branch, verifies state, returns status=passed
    - [ROLL-01] run_rollback_drill with no prior tag returns status=skipped with reason=no_baseline_tag
    - [ROLL-01] run_rollback_drill cleans up temp branch after success (no orphan branches)
    - [ROLL-01] run_rollback_drill cleans up temp branch after failure (trap-based cleanup)
    - [ROLL-01] run_rollback_drill writes evidence artifact to .aegis/evidence/rollback-drill-phase-{N}.json
    - [ROLL-01] Evidence artifact contains: status, phase, baseline_tag, state_recoverable, compatibility, timestamp
    - [ROLL-01] run_rollback_drill returns proper JSON on stdout for advance stage consumption
  </behavior>
  <action>
    Write tests first in `tests/test-rollback-drill.sh`:
    - Follow test-regression.sh pattern: create temp git repos with `git init`, create tags, verify drill behavior
    - Setup: create temp dir, `git init`, create a commit, tag it as `aegis/phase-{N}-test`, create another commit (simulating current phase work)
    - Test no-prior-tag case: fresh repo with no aegis tags
    - Test passed case: repo with prior tag, verify JSON output has status=passed
    - Test cleanup: verify no `rollback-drill-*` branches remain after drill
    - Test evidence: verify `.aegis/evidence/rollback-drill-phase-{N}.json` exists after drill
    - Prefix assertions with `[ROLL-01]`

    Then create `lib/aegis-rollback-drill.sh`:
    - Follow `aegis-regression.sh` conventions: source aegis-git.sh and aegis-evidence.sh
    - `run_rollback_drill(current_phase)`: implements the drill flow from 16-RESEARCH.md
    - Use `trap` on RETURN for cleanup (delete temp branch, restore original branch)
    - Find prior tag: `git tag -l "aegis/phase-${prev_phase}-*" | sort -V | tail -1`
    - No prior tag: return `{"status": "skipped", "reason": "no_baseline_tag"}` and return 0
    - Drill steps: save current branch, create temp branch from prior tag, verify state file at tag, check rollback compatibility, cleanup, write evidence artifact
    - Handle dirty working tree: use `git stash` before checkout if needed, `git stash pop` after
    - Write evidence via `write_evidence()` with stage="rollback-drill"

    Run tests -- RED then GREEN.
  </action>
  <verify>
    <automated>bash tests/test-rollback-drill.sh</automated>
  </verify>
  <done>All rollback drill tests pass. Drill creates/cleans temp branch, skips gracefully without prior tag, writes evidence artifact, all [ROLL-01] assertions green.</done>
</task>

</tasks>

<verification>
```bash
bash tests/test-patterns.sh && bash tests/test-rollback-drill.sh
```
Both test suites pass. No orphan branches left. Pattern files written to correct location.
</verification>

<success_criteria>
- lib/aegis-patterns.sh exports save_pattern, approve_pattern, list_patterns, get_pattern
- lib/aegis-rollback-drill.sh exports run_rollback_drill
- tests/test-patterns.sh covers PATN-01, PATN-03 with all assertions passing
- tests/test-rollback-drill.sh covers ROLL-01 with all assertions passing
- Both libraries follow bash+python3 conventions from existing codebase
</success_criteria>

<output>
After completion, create `.planning/phases/16-patterns-and-rollback/16-01-SUMMARY.md`
</output>
