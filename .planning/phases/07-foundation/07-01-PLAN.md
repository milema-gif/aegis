---
phase: 07-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/aegis-state.sh
  - scripts/aegis
  - tests/test-complete-stage.sh
  - tests/test-namespace.sh
  - tests/run-all.sh
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03

must_haves:
  truths:
    - "Calling complete_stage('research') marks the stage completed with a timestamp, and calling it again is a no-op returning 0"
    - "Subagents from different stages cannot read or write each other's working state"
    - "The 'aegis' command is available on PATH from any directory"
  artifacts:
    - path: "lib/aegis-state.sh"
      provides: "complete_stage() and ensure_stage_workspace() functions"
      contains: "complete_stage"
    - path: "scripts/aegis"
      provides: "Global wrapper script for PATH availability"
    - path: "tests/test-complete-stage.sh"
      provides: "FOUND-01 tests (idempotency, atomicity, unknown stage)"
    - path: "tests/test-namespace.sh"
      provides: "FOUND-02 tests (workspace creation, isolation)"
  key_links:
    - from: "lib/aegis-state.sh"
      to: ".aegis/state.current.json"
      via: "python3 JSON manipulation with tmp+mv atomic write"
      pattern: "complete_stage.*python3"
    - from: "lib/aegis-state.sh"
      to: ".aegis/workspaces/{stage}/"
      via: "ensure_stage_workspace() creates isolated directories"
      pattern: "ensure_stage_workspace"
    - from: "scripts/aegis"
      to: "skills/aegis-launch.md"
      via: "exec claude with skill-file argument"
      pattern: "AEGIS_HOME"
---

<objective>
Add pipeline foundation infrastructure: `complete_stage()` helper for reliable stage completion signals, subagent namespace isolation via stage-scoped workspaces, and global PATH installation wrapper.

Purpose: Phase 8 (checkpoints) and Phase 10 (deploy preflight) both depend on a clean `complete_stage()` signal. Namespace isolation prevents cross-stage state pollution. Global install removes path gymnastics for hooks and scripts.
Output: Updated `lib/aegis-state.sh` with two new functions, `scripts/aegis` wrapper, two new test files, updated test runner.
</objective>

<execution_context>
@/home/ai/.claude/get-shit-done/workflows/execute-plan.md
@/home/ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-foundation/07-RESEARCH.md

@lib/aegis-state.sh
@tests/run-all.sh
@tests/test-memory-stub.sh
</context>

<interfaces>
<!-- Existing patterns the executor needs. -->

From lib/aegis-state.sh:
```bash
# Atomic write pattern (use this for complete_stage):
write_state() {
  local json_content="${1:?write_state requires json_content}"
  mkdir -p "$AEGIS_DIR"
  echo "$json_content" > "${AEGIS_DIR}/state.current.json.tmp.$$"
  mv -f "${AEGIS_DIR}/state.current.json.tmp.$$" "${AEGIS_DIR}/state.current.json"
}

# Stage list:
STAGES=("intake" "research" "roadmap" "phase-plan" "execute" "verify" "test-gate" "advance" "deploy")

# AEGIS_DIR default:
AEGIS_DIR="${AEGIS_DIR:-.aegis}"
```

From tests/test-memory-stub.sh (test pattern):
```bash
pass() { echo "PASS: $1"; PASS_COUNT=$((PASS_COUNT + 1)); }
fail() { echo "FAIL: $1 — $2"; FAIL_COUNT=$((FAIL_COUNT + 1)); }
setup() { TEST_DIR=$(mktemp -d); export AEGIS_DIR="$TEST_DIR/.aegis"; }
teardown() { rm -rf "$TEST_DIR"; }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add complete_stage() and ensure_stage_workspace() to aegis-state.sh</name>
  <files>lib/aegis-state.sh, tests/test-complete-stage.sh, tests/test-namespace.sh, tests/run-all.sh</files>
  <behavior>
    - Test: complete_stage("research") on a fresh state sets status="completed" and completed_at to ISO timestamp
    - Test: complete_stage("research") called twice returns 0 both times, completed_at unchanged on second call (idempotent)
    - Test: complete_stage("nonexistent-stage") returns exit code 1 and prints error to stderr
    - Test: complete_stage() with no argument fails (requires stage_name)
    - Test: ensure_stage_workspace("execute") creates .aegis/workspaces/execute/ directory and returns the path
    - Test: ensure_stage_workspace("execute") is idempotent (second call succeeds, same path)
    - Test: Files written to workspace A are not visible from workspace B (isolation)
  </behavior>
  <action>
    1. Create `tests/test-complete-stage.sh` following the existing test pattern from `test-memory-stub.sh` (setup/teardown with tmpdir, pass/fail counters, source the lib). Tests must init state first using `init_state "test-project"` to create the state file, then exercise `complete_stage()`. For idempotency test, capture completed_at after first call, call again, verify completed_at unchanged.

    2. Create `tests/test-namespace.sh` following same pattern. Tests create workspaces, write files to one workspace, verify they are not visible from another workspace path.

    3. Add `complete_stage()` to `lib/aegis-state.sh` after the existing `advance_stage()` function:
       - Takes one required argument: `stage_name`
       - Reads `state.current.json`, finds the stage by name
       - If stage already has `status == "completed"`, exit 0 immediately (idempotent no-op)
       - If stage name not found, print error to stderr and return 1
       - Set `status = "completed"` and `completed_at = now` (ISO 8601 UTC)
       - Update `updated_at` on root object
       - Write atomically via tmp+mv pattern (same as `write_state()`)
       - Do NOT use `write_state()` directly -- use the python3 inline approach from `advance_stage()` that reads, modifies, and writes in one python3 call

    4. Add `ensure_stage_workspace()` to `lib/aegis-state.sh`:
       - Takes one required argument: `stage_name`
       - Creates `$AEGIS_DIR/workspaces/$stage_name/` if it doesn't exist
       - Echoes the workspace path to stdout
       - Returns 0

    5. Update `tests/run-all.sh`: add "test-complete-stage" and "test-namespace" to the TESTS array, after "test-consultation" and before "test-pipeline-integration".
  </action>
  <verify>
    <automated>bash tests/test-complete-stage.sh && bash tests/test-namespace.sh && bash tests/run-all.sh</automated>
  </verify>
  <done>
    - complete_stage() passes idempotency, atomicity, and unknown-stage-rejection tests
    - ensure_stage_workspace() passes creation and isolation tests
    - Full test suite (run-all.sh) passes with new tests included
  </done>
</task>

<task type="auto">
  <name>Task 2: Create global aegis wrapper script and install to PATH</name>
  <files>scripts/aegis</files>
  <action>
    1. Create `scripts/aegis` as a POSIX-compatible wrapper script:
       ```
       #!/usr/bin/env bash
       # aegis - Global wrapper for Aegis pipeline
       # Delegates to Claude Code skill launcher
       AEGIS_HOME="/home/ai/aegis"
       exec claude --skill-file "$AEGIS_HOME/skills/aegis-launch.md" "$@"
       ```
       Make it executable: `chmod +x scripts/aegis`

    2. Create symlink: `ln -sf /home/ai/aegis/scripts/aegis /home/ai/bin/aegis`
       (Create `~/bin` if it doesn't exist and verify it's on PATH)

    3. If `~/bin` is not on PATH, also create symlink at `/usr/local/bin/aegis` as fallback.

    4. Verify: `which aegis` returns a path, and the script is syntactically valid (`bash -n scripts/aegis` passes).

    Note: Do NOT test `aegis --help` execution because it would launch Claude Code. Only verify the script exists on PATH and is syntactically valid.
  </action>
  <verify>
    <automated>bash -n /home/ai/aegis/scripts/aegis && test -x /home/ai/aegis/scripts/aegis && (which aegis || test -L /home/ai/bin/aegis)</automated>
  </verify>
  <done>
    - `scripts/aegis` exists and is executable
    - `aegis` is resolvable on PATH via symlink
    - Script passes syntax check
  </done>
</task>

</tasks>

<verification>
- `bash tests/run-all.sh` passes with all tests including new ones
- `complete_stage()` is idempotent (call twice, same result)
- `ensure_stage_workspace()` creates isolated directories
- `aegis` wrapper is on PATH and syntactically valid
</verification>

<success_criteria>
- All FOUND-01, FOUND-02, FOUND-03 requirements satisfied
- No regressions in existing 13 test suites
- Foundation functions available for Plan 02 (memory scoping) and downstream phases
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundation/07-01-SUMMARY.md`
</output>
