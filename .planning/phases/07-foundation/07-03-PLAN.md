---
phase: 07-foundation
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - lib/aegis-memory.sh
  - scripts/aegis-migrate-memory.sh
  - tests/test-memory-scoping.sh
  - tests/test-memory-migration.sh
  - tests/run-all.sh
autonomous: false
requirements:
  - MEM-05
  - MEM-07

must_haves:
  truths:
    - "Memory decay runs at startup with a 24h guard so it only executes once per day"
    - "Pinned memories never decay regardless of age"
    - "Session memories older than 30 days are removed by decay"
    - "Ephemeral memories older than 7 days are removed by decay"
    - "Legacy 424 Engram observations are classified by project before scoping enforcement ships"
  artifacts:
    - path: "lib/aegis-memory.sh"
      provides: "memory_decay() with class-based policy and 24h guard"
      contains: "memory_decay"
    - path: "scripts/aegis-migrate-memory.sh"
      provides: "Legacy migration script with auto-classify + operator review"
    - path: "tests/test-memory-migration.sh"
      provides: "MEM-05 migration tests (dry-run, classification)"
  key_links:
    - from: "lib/aegis-memory.sh"
      to: ".aegis/memory/"
      via: "memory_decay() scans entries, checks decay_class and age"
      pattern: "memory_decay"
    - from: "scripts/aegis-migrate-memory.sh"
      to: "Engram MCP (mem_search/mem_save)"
      via: "Dumps existing observations, classifies, re-saves with project tags"
      pattern: "aegis-migrate"
---

<objective>
Add memory decay with class-based policy and build the legacy memory migration script for 424 existing Engram observations.

Purpose: Memory decay prevents unbounded growth without destroying stable architectural decisions (class-based policy). Legacy migration ensures the 424 existing unscoped observations are classified before MEM-04 scoping enforcement blocks untagged writes. Migration requires operator review checkpoint.
Output: Updated `lib/aegis-memory.sh` with decay function, new migration script, migration test file.
</objective>

<execution_context>
@/home/ai/.claude/get-shit-done/workflows/execute-plan.md
@/home/ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-foundation/07-RESEARCH.md
@.planning/phases/07-foundation/07-02-SUMMARY.md

@lib/aegis-memory.sh
@references/memory-taxonomy.md
</context>

<interfaces>
<!-- From Plan 02 output -- the scoped memory API this plan extends. -->

From lib/aegis-memory.sh (after Plan 02):
```bash
memory_save_scoped(project, scope, key, content, cross_project)
memory_save_gate(project, stage, phase, summary)
memory_pollution_scan(project)
memory_retrieve_context_scoped(project, terms, limit)
```

Decay classes (from updated memory-taxonomy.md):
```
| Class     | TTL        | Policy                                    |
|-----------|------------|-------------------------------------------|
| pinned    | never      | Architectural decisions -- never expire    |
| project   | on archive | Active project memories -- decay on archive|
| session   | 30 days    | Session-specific context -- auto-decay     |
| ephemeral | 7 days     | Temporary working state -- auto-decay      |
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add memory_decay() with class-based policy and 24h startup guard</name>
  <files>lib/aegis-memory.sh, tests/test-memory-scoping.sh</files>
  <behavior>
    - Test: memory_decay() skips entries with decay_class="pinned" regardless of age
    - Test: memory_decay() removes entries with decay_class="ephemeral" older than 7 days
    - Test: memory_decay() removes entries with decay_class="session" older than 30 days
    - Test: memory_decay() keeps entries with decay_class="session" younger than 30 days
    - Test: memory_decay() does not run if .aegis/memory/.last_decay is less than 24h old (guard)
    - Test: memory_decay() updates .aegis/memory/.last_decay timestamp after running
    - Test: Entries without decay_class default to "project" class (not decayed unless archived)
  </behavior>
  <action>
    1. Add decay tests to `tests/test-memory-scoping.sh` (append to existing test file from Plan 02). Create test entries with timestamps in the past using `date -d` for GNU date. Test the 24h guard by creating a `.last_decay` file with a recent timestamp.

    2. Modify `memory_save()` in `lib/aegis-memory.sh` to accept an optional `decay_class` parameter (5th positional arg, default "project"). Store it in the JSON entry alongside key, content, and timestamp.

    3. Update `memory_save_scoped()` to pass through a `decay_class` parameter (6th positional arg after `cross_project`, default "project").

    4. Add `memory_decay()` to `lib/aegis-memory.sh`:
       - Parameter: `project` (required)
       - 24h guard: Check `$MEMORY_DIR/.last_decay` file. If it exists and its modification time is less than 24 hours ago, return 0 (skip). Use `find "$MEMORY_DIR/.last_decay" -mmin -1440` to check.
       - Scan all `${project}-*.json` files in `$MEMORY_DIR/`
       - For each entry, read `decay_class` (default "project" if missing) and `timestamp`
       - Policy:
         - `pinned`: never remove
         - `project`: never remove (removed only on archive, which is a separate operation)
         - `session`: remove if older than 30 days
         - `ephemeral`: remove if older than 7 days
       - Use python3 to filter entries and rewrite the JSON file atomically (tmp+mv)
       - Print count of decayed entries to stdout
       - Update `.last_decay` guard file: `touch "$MEMORY_DIR/.last_decay"`
  </action>
  <verify>
    <automated>bash tests/test-memory-scoping.sh</automated>
  </verify>
  <done>
    - memory_decay() respects all four class policies
    - 24h guard prevents excessive decay runs
    - Entries without decay_class treated as "project" (safe default)
    - Existing scoping tests from Plan 02 still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create legacy memory migration script</name>
  <files>scripts/aegis-migrate-memory.sh, tests/test-memory-migration.sh, tests/run-all.sh</files>
  <action>
    1. Create `tests/test-memory-migration.sh` following standard test pattern. Tests should:
       - Create a mock memory directory with sample entries (some with project keywords, some generic)
       - Run migration script in dry-run mode and verify it produces a classification report
       - Verify auto-classification identifies entries containing known project names (aegis, worldmonitor, radiantreport, etc.)
       - Verify unclassified entries are tagged as "unclassified" (treated as pinned/global)

    2. Create `scripts/aegis-migrate-memory.sh`:
       - `#!/usr/bin/env bash` with `set -euo pipefail`
       - Accepts flags: `--dry-run` (report only, no writes), `--auto` (auto-classify without review)
       - Default mode (no flags): auto-classify then present unclassified for operator review

       **Auto-classification logic:**
       - Known project keywords: aegis, worldmonitor, radiantreport, aus-tracker, whisker, home-dashboard, gpu-box, infrastructure, chrome, seismic
       - For each local memory entry, scan key + content for project keywords
       - If exactly one project keyword matches, classify to that project
       - If multiple match or none match, mark as "unclassified"

       **Dry-run mode:**
       - Print classification report: `{count} entries classified, {count} unclassified`
       - List each entry with its proposed classification
       - Exit without writing

       **Interactive mode (default):**
       - Auto-classify first
       - Present unclassified entries one-by-one for operator input
       - Accept project name or "global" or "skip" for each
       - After review, re-save all classified entries using `memory_save_scoped()` with project prefix
       - Unclassified entries left as-is (treated as pinned/global until classified)

       **Note on Engram MCP migration:** The script handles LOCAL JSON memory files. Engram MCP observations are managed via the Engram tools at conversation level. This script documents how to handle Engram migration but does not directly invoke MCP tools (those run in Claude conversation context). Include a comment block explaining the two-phase approach: (1) this script migrates local JSON, (2) Engram MCP migration is a manual `mem_search` + `mem_save` operation during a dedicated session.

    3. Make executable: `chmod +x scripts/aegis-migrate-memory.sh`

    4. Update `tests/run-all.sh`: add "test-memory-migration" to TESTS array after "test-memory-scoping".
  </action>
  <verify>
    <automated>bash tests/test-memory-migration.sh && bash tests/run-all.sh</automated>
  </verify>
  <done>
    - Migration script exists and passes dry-run tests
    - Auto-classification correctly identifies project keywords
    - Unclassified entries are safely preserved (not deleted)
    - Full test suite passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify migration output and approve legacy classification</name>
  <files>scripts/aegis-migrate-memory.sh</files>
  <action>
    What was built: Memory decay system (class-based, 24h guard) and legacy migration script (auto-classify + operator review). The migration script classifies existing local memory entries by project using keyword matching.

    How to verify:
    1. Run migration dry-run to see classification report:
       `bash scripts/aegis-migrate-memory.sh --dry-run`
    2. Review the output: verify auto-classifications look correct
    3. If satisfied, run actual migration:
       `bash scripts/aegis-migrate-memory.sh`
    4. Review any "unclassified" entries presented for manual classification
    5. After migration, verify scoped files exist in `.aegis/memory/`:
       `ls .aegis/memory/`
    6. Run full test suite to confirm no regressions:
       `bash tests/run-all.sh`

    Resume signal: Type "approved" if migration output is acceptable, or describe issues with specific entries.
  </action>
  <verify>
    <automated>bash tests/run-all.sh</automated>
  </verify>
  <done>Operator has reviewed and approved the legacy memory classification</done>
</task>

</tasks>

<verification>
- `bash tests/test-memory-scoping.sh` passes all tests including decay
- `bash tests/test-memory-migration.sh` passes dry-run classification tests
- `bash tests/run-all.sh` passes with all new test files
- Migration script produces sensible auto-classifications in dry-run mode
</verification>

<success_criteria>
- MEM-07: Decay runs at startup with 24h guard, respects all four decay classes
- MEM-05: Migration script classifies legacy observations, operator reviews unclassified entries
- No regressions in existing tests
- Memory system is fully project-scoped end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundation/07-03-SUMMARY.md`
</output>
