---
phase: 11-policy-as-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aegis-policy.json
  - templates/aegis-policy.default.json
  - lib/aegis-policy.sh
  - tests/test-policy-config.sh
autonomous: true
requirements: [POLC-01, POLC-02]

must_haves:
  truths:
    - "A versioned policy config file defines all gate behavior per stage"
    - "Policy loader validates config at startup and fails fast on errors"
    - "Policy version is retrievable for downstream stamping"
    - "Default policy file ships as a reference template"
  artifacts:
    - path: "aegis-policy.json"
      provides: "Gate policy configuration"
      contains: "policy_version"
    - path: "templates/aegis-policy.default.json"
      provides: "Shipped default policy for reset/reference"
      contains: "policy_version"
    - path: "lib/aegis-policy.sh"
      provides: "Policy loader and utility functions"
      exports: ["load_policy", "get_policy_version", "get_gate_config", "get_consultation_config", "stamp_policy_version", "validate_policy"]
    - path: "tests/test-policy-config.sh"
      provides: "Policy config unit and integration tests"
      min_lines: 100
  key_links:
    - from: "lib/aegis-policy.sh"
      to: "aegis-policy.json"
      via: "load_policy reads and validates"
      pattern: "AEGIS_POLICY_FILE"
    - from: "lib/aegis-policy.sh"
      to: "AEGIS_POLICY_VERSION"
      via: "get_policy_version exports cached version"
      pattern: "get_policy_version"
---

<objective>
Create the policy-as-code config file and loader library.

Purpose: Extract gate policies into a versioned, git-tracked JSON config that the pipeline reads at startup. This is the foundation that Plan 02 wires into existing gate evaluation and consultation code.
Output: aegis-policy.json (the config), templates/aegis-policy.default.json (shipped default), lib/aegis-policy.sh (loader + utilities), tests/test-policy-config.sh (test coverage)
</objective>

<execution_context>
@/home/ai/.claude/get-shit-done/workflows/execute-plan.md
@/home/ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-policy-as-code/11-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor needs to follow -->

From lib/aegis-state.sh:
```bash
AEGIS_DIR="${AEGIS_DIR:-.aegis}"
AEGIS_TEMPLATE_DIR="${AEGIS_TEMPLATE_DIR:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../templates" && pwd)}"
STAGES=("intake" "research" "roadmap" "phase-plan" "execute" "verify" "test-gate" "advance" "deploy")
```

From templates/pipeline-state.json (current gate fields per stage):
```json
{
  "gate": { "type": "approval", "skippable": true, "max_retries": 0, "backoff": "none", "timeout_seconds": 0, "attempts": 0, "first_attempt_at": null, "last_result": null, "last_error": null, "pending_approval": false }
}
```

Test pattern from tests/test-gate-evaluation.sh:
```bash
PASS_COUNT=0; FAIL_COUNT=0
pass() { echo "PASS: $1"; PASS_COUNT=$((PASS_COUNT + 1)); }
fail() { echo "FAIL: $1 — $2"; FAIL_COUNT=$((FAIL_COUNT + 1)); }
setup() { TEST_DIR=$(mktemp -d); export AEGIS_DIR="$TEST_DIR/.aegis"; ... }
teardown() { rm -rf "$TEST_DIR"; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create policy config, default template, and test scaffold</name>
  <files>aegis-policy.json, templates/aegis-policy.default.json, tests/test-policy-config.sh</files>
  <behavior>
    - Test: aegis-policy.json exists and is valid JSON
    - Test: aegis-policy.json has policy_version field
    - Test: aegis-policy.json has gate config for all 9 stages (intake, research, roadmap, phase-plan, execute, verify, test-gate, advance, deploy)
    - Test: each gate config has required fields (type, skippable, max_retries, backoff, timeout_seconds)
    - Test: aegis-policy.json has consultation config for all 9 stages
    - Test: each consultation config has type and context_limit fields
    - Test: aegis-policy.json has gate_rules section
    - Test: gate types are valid (approval, quality, external, cost, none)
    - Test: backoff values are valid (none, fixed-5s, exp-5s)
    - Test: default template matches aegis-policy.json content
  </behavior>
  <action>
    1. Create `aegis-policy.json` in project root with the exact schema from RESEARCH.md Pattern 1. Use the values from the current `templates/pipeline-state.json` gate fields and `lib/aegis-consult.sh` case statement to ensure behavioral parity:
       - gates: 9 stages, each with type/skippable/max_retries/backoff/timeout_seconds matching current hardcoded values
       - consultation: 9 stages, each with type/context_limit matching current case statement (intake=none/0, research=routine/2000, roadmap=routine/2000, phase-plan=routine/2000, execute=none/0, verify=critical/4000, test-gate=none/0, advance=none/0, deploy=critical/4000)
       - gate_rules: quality_never_skippable=true, external_never_skippable=true, compound_evaluation="left-to-right-short-circuit"
       - policy_version: "1.0.0", description, updated_at
    2. Copy `aegis-policy.json` to `templates/aegis-policy.default.json` (identical content — the shipped default for reference/reset).
    3. Create `tests/test-policy-config.sh` with the test scaffold following existing test patterns (pass/fail/setup/teardown). Write all tests from the behavior block FIRST, expecting them to test both the config files and (in Task 2) the loader library. Tests that depend on lib/aegis-policy.sh should be written but will pass only after Task 2.
  </action>
  <verify>
    <automated>cd /home/ai/aegis && python3 -c "import json; json.load(open('aegis-policy.json'))" && python3 -c "import json; json.load(open('templates/aegis-policy.default.json'))" && bash -n tests/test-policy-config.sh</automated>
  </verify>
  <done>aegis-policy.json and templates/aegis-policy.default.json exist with valid JSON matching current gate behavior. tests/test-policy-config.sh exists with valid bash syntax and test cases for all POLC requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create policy loader library (lib/aegis-policy.sh)</name>
  <files>lib/aegis-policy.sh, tests/run-all.sh</files>
  <behavior>
    - Test: load_policy succeeds with valid aegis-policy.json
    - Test: load_policy fails (exit 1) when policy file is missing
    - Test: load_policy fails when policy_version is missing from config
    - Test: load_policy fails when a required stage is missing from gates
    - Test: load_policy fails when a required gate field is missing
    - Test: load_policy fails when gate type is invalid
    - Test: load_policy fails when backoff value is invalid
    - Test: get_policy_version returns the policy_version string after load_policy
    - Test: get_gate_config returns correct JSON for a given stage
    - Test: get_consultation_config returns correct JSON for a given stage
    - Test: stamp_policy_version adds policy_version to a JSON file
    - Test: validate_policy returns errors list for malformed config
  </behavior>
  <action>
    1. Create `lib/aegis-policy.sh` following the project's bash library pattern (set -euo pipefail, AEGIS_LIB_DIR variable, python3 for JSON). Implement:
       - `AEGIS_POLICY_FILE` env var default: look for `aegis-policy.json` in the project root (use `$AEGIS_LIB_DIR/..` to find it, or accept override via env var)
       - `AEGIS_POLICY_VERSION=""` global
       - `load_policy()`: validates the config file exists, runs `validate_policy` inline via python3, extracts and exports `AEGIS_POLICY_VERSION`. Fails fatally (return 1) on any error. Validates: required top-level fields (policy_version, gates, consultation), all 9 stages present in both gates and consultation, required gate fields per stage (type, skippable, max_retries, backoff, timeout_seconds), valid gate types (approval, quality, external, cost, none — compound comma-separated allowed), valid backoff values (none, fixed-5s, exp-5s).
       - `get_policy_version()`: echoes `$AEGIS_POLICY_VERSION`
       - `get_gate_config(stage_name)`: uses python3 to read `$AEGIS_POLICY_FILE` and print the gate config JSON for the given stage
       - `get_consultation_config(stage_name)`: uses python3 to read `$AEGIS_POLICY_FILE` and print the consultation config JSON for the given stage
       - `stamp_policy_version(artifact_file)`: uses python3 to read the given JSON file, add `policy_version` field from `$AEGIS_POLICY_VERSION`, write it back (atomic tmp+mv pattern)
       - `validate_policy()`: standalone validation callable for testing — prints errors to stderr, returns 1 if any
    2. Add `test-policy-config` to the TESTS array in `tests/run-all.sh` (after "test-consultation" and before "test-complete-stage").
    3. Run the full test suite: `bash tests/test-policy-config.sh` — all tests from Task 1 should now pass since the loader library exists.
  </action>
  <verify>
    <automated>cd /home/ai/aegis && bash tests/test-policy-config.sh</automated>
  </verify>
  <done>lib/aegis-policy.sh exists with all 6 functions. load_policy validates config and fails fast on errors. tests/test-policy-config.sh passes all tests. test-policy-config is registered in run-all.sh.</done>
</task>

</tasks>

<verification>
- `bash tests/test-policy-config.sh` — all policy tests pass
- `python3 -c "import json; p=json.load(open('aegis-policy.json')); assert p['policy_version']=='1.0.0'; assert len(p['gates'])==9; assert len(p['consultation'])==9"` — config structure valid
- `bash tests/run-all.sh` — existing tests still pass (no regressions)
</verification>

<success_criteria>
- aegis-policy.json exists in project root with versioned gate/consultation/rules config for all 9 stages
- lib/aegis-policy.sh provides load_policy, get_policy_version, get_gate_config, get_consultation_config, stamp_policy_version, validate_policy
- Policy file is git-tracked (POLC-02)
- All new tests pass, no regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/11-policy-as-code/11-01-SUMMARY.md`
</output>
